# Graph3D.js — v2 Build Plan

Source of truth for architecture layers and build phases. Each prompt number in `Promps.md` maps to a deliverable; each phase below tracks completion against its exit criteria.

---

## 10-Layer Architecture

Layers are ordered bottom-to-top. A layer may only import from layers **below** it.

| # | Layer | Responsibility |
|---|-------|----------------|
| 1 | **Core Engine** | `Graph3D`, `Graph3DRenderer`, `Graph3DLoop`, `Graph3DRegistry`, `CapabilityProbe`, `FrameBudget`, `WorkerPool`, worker bootstrap. |
| 2 | **Scene Composition** | `GraphScene` wrapper, cameras (orbit/isometric/cinematic), light rigs, HDR environment, shadows, fog, clip planes. |
| 3 | **Object & Mesh** | `GraphObject`, `GraphInstancedObject` (default for >50 datums), loader, octree spatial index. |
| 4 | **Compositional Core** | D3-flavored scales, generators, layouts, color palettes, axes, annotations. No Three.js — operates on plain arrays/numbers. |
| 5 | **Animation & Transitions** | Anim engine, timelines, easing curves, data transitions, camera tours. |
| 6 | **Materials & Procedural FX** | PBR presets, SDF text, gradient/noise procedural textures, named beautiful materials. |
| 7 | **PostFX & Particles** | Bloom, SSAO, DOF, motion blur, particle systems, god rays. Charts request passes through a public PostFX API. |
| 8 | **Chart Types** | Bar, Line, Scatter, Surface, Heatmap, Network, Tree, Pack — all instanced by default. Fluent chainable API. |
| 9 | **Interaction & State** | Picking, hover/focus/select state machine, brush, lasso, cross-filter, tooltips. |
| 10 | **Streaming & Scale** | `DataStream`, GPGPU, LOD, aggregation pipelines, origin-shifting for large coordinate ranges. |

---

## 13 Build Phases

### Phase 0 — Bootstrap & Tooling ✅
**Scope:** project skeleton, toolchain, CI, playground  
**Prompts:** 1–8  
**Exit criteria:**
- [x] `npm run dev` boots with an empty playground and zero console errors
- [x] `npm run test` runs in jsdom (Vitest)
- [x] `npm run build` produces ESM + UMD bundles with `three` external
- [x] `npm run lint` exits 0
- [x] `.github/workflows/ci.yml` covers lint + test + build + bundle-size check
- [x] `bench/harness.js` wired to `npm run bench`

---

### Phase 1 — Core Engine ✅
**Scope:** `src/core/`  
**Prompts:** 9–20  
**Exit criteria:**
- [x] `CapabilityProbe` detects WebGL2, instanced arrays, float textures, timer queries, GPU vendor; exposes frozen `Capabilities` object; gracefully falls back to `NULL_CAPABILITIES` when WebGL is unavailable
- [x] `Graph3DRenderer` wraps `THREE.WebGLRenderer`; configures sRGB + ACESFilmic tonemapping + PCFSoft shadows by default; emits `graph3d:context-lost` / `graph3d:context-restored` on canvas; `dispose()` removes all listeners
- [x] `Graph3DLoop` is a singleton RAF manager; single RAF per page; auto-starts on first `add`, auto-stops on last `remove`; pauses on `visibilitychange`; callbacks receive `(deltaSec, elapsedSec)`
- [x] `Graph3DRegistry` tracks all live instances; `disposeAll`, `pauseAll`, `resumeAll`, `panicDispose` all work
- [x] `FrameBudget` emits `graph3d:slow-frame` after `windowSize` consecutive over-budget frames; resets counter after emit; configurable `budgetMs` and `windowSize`
- [x] `WorkerPool` creates workers lazily via injected `workerFactory`; `exec(taskName, payload, transferList?)` returns a Promise; queues tasks when all workers are busy; terminates idle workers after 30 s; `dispose()` rejects in-flight tasks
- [x] Worker bootstrap is base64-inlined as a Blob URL (no CORS / path issues for library consumers); `registerWorkerTask` serialises handler functions into the worker
- [x] `Graph3D` top-level class composes all of the above; `autoResize` via `ResizeObserver`; `pause()` / `resume()`; `chart()` shell; `disposeAll` + `version` statics; fully idempotent `dispose()`
- [x] Unit tests cover every public method (`tests/core/*.test.js`) — 247 tests, 10 files, all passing
- [x] Integration tests confirm end-to-end behaviour (`tests/integration/phase1.test.js`): 1000× construct/dispose, tick delta conversion, pause/resume loop state, context-loss simulation, slow-frame event chain
- [x] No `console.log` in source; no magic numbers without named constants; all public methods have JSDoc
- [x] `docs/concepts/core.md` documents the single-RAF design, capability probe, frame budget, and worker pool

---

### Phase 2 — Scene Composition ✅
**Scope:** `src/scene/`  
**Prompts:** 21–30  
**Exit criteria:**
- [x] `GraphScene` wraps `THREE.Scene`; `add`, `remove`, `traverse`, `findByName`, `dispose` (walks geometry/material/texture disposal — sets the disposal standard for all future phases); `selectByName`/`selectInstance` (Prompt 47) look up registered `GraphObject`/`GraphInstancedObject` wrappers via the per-scene registry in `GraphSceneRegistry.js` — kept in `scene/` rather than `object/` so `GraphScene` never has to import concrete wrapper subclasses (see `GraphObject.isInstanced`/`GraphInstancedObject.capacity`)
- [x] `GraphSceneCamera` wraps perspective and orthographic cameras; presets: `orbit`, `fixed`, `isometric`, `top-down`, `cinematic-low`, `cinematic-high`; lazy-loads `OrbitControls`
- [x] Cinematic camera primitives: `dollyZoom`, `tour(waypoints)`, `follow(target)`, `focusOn(boundingBox)`; each returns a chainable controller with `.cancel()`
- [x] `GraphSceneLight` implements presets: `ambient-only`, `three-point`, `studio`, `flat`, `cinematic`, `product-shot`
- [x] `GraphSceneShadows` configures PCF / PCFSoft / VSM shadow maps and quality levels
- [x] `GraphSceneEnvironment` loads HDR via `RGBELoader`; ref-counted texture cache; fog presets; skybox; built-in preset names `studio-1k`, `cinema-night`, `daylight` wired to `src/scene/env/*.hdr` (binary assets not yet bundled — see `.claude/TODO.md`)
- [x] `Graph3D.createScene(name)` and `setActiveScene(nameOrScene)` wired; per-frame tick renders `activeScene`
- [x] Disposal tests: create + dispose N scenes; `renderer.info.memory` returns to baseline
- [x] `docs/concepts/scene.md` documents the scene graph, camera presets, and HDR environment

---

### Phase 3 — Object & Mesh
**Scope:** `src/object/`  
**Prompts:** 31–40  
**Exit criteria:**
- [x] `GraphObject` base wrapper: holds `THREE.Object3D`, exposes `dispose()`, supports user-defined metadata
- [x] `GraphInstancedObject` extends `GraphObject` for instanced rendering (>50 datums threshold); per-instance setters for matrix/position/rotation/scale/color/user data (Prompt 37), custom shader attributes (Prompt 38), a stable `instanceId` attribute + octree-backed `pick(raycaster)` (Prompt 39, upgraded Prompt 45), throttled per-instance frustum culling via `enableInstanceCulling`/`updateCulling`, octree-backed and live-updating as instances move (Prompt 40, upgraded Prompt 45), and a capacity-grow strategy (Prompt 49): `setInstanceCount(n)` beyond capacity reallocates at `ceilPowerOfTwo(n)` and copies every attribute (including custom ones from Prompt 38), preserving the octree index mapping
- [x] `Octree` (Prompt 44): `insert`/`remove`/`queryFrustum`/`queryRay`/`queryRadius`/`queryAABB`, spatial index for fast frustum culling and picking on large instance sets — wired into `GraphInstancedObject` in Prompt 45, updated incrementally by every `setInstanceMatrix`/`Position`/`Rotation`/`Scale` call
- [x] Loader (`GraphObjectLoader`, Prompt 43) supports GLTF/GLB (+ Draco/KTX2, config-gated — see `.claude/TODO.md`), OBJ (+ MTL), and FBX; ref-counted per-URL cache avoids re-fetching, each caller gets an independent disposable clone; progress-event callbacks not yet exposed (not required by Prompt 43's own spec)
- [x] Disposal tests: instance buffer attributes return to baseline count after dispose (`tests/integration/GraphInstancedObject-disposal.test.js`); a 1,000,000-instance create+dispose cycle is covered leak-free by `tests/integration/phase3.test.js` (Prompt 52)
- [x] `GraphMesh extends GraphObject` (Prompt 42): individual-mesh transform/vertex mutation API, `clone()`/`deepClone()`
- [x] `GraphObjectFactory` (Prompt 41): `createBars`/`createPoints`/`createLineSegments`/`createSurfaceTiles`/`createNodes`, dispatching on `count` vs. the configurable `INSTANCING_THRESHOLD` (default 50) to either `GraphMesh[]` or one `GraphInstancedObject`
- [x] Examples: `examples/03-instanced/main.js` (100,000 shader-spun bars, Prompt 50) and `examples/03-million/main.js` (1,000,000 noise-placed point spheres, octree-backed hover picking, Prompt 51)
- [x] Phase 3 integration tests (Prompt 52): 1M instance create+dispose leak-free; instance picking correct for known positions; octree matches brute-force on 10K points; capacity grow preserves all attributes; ≤50 → meshes / >50 → InstancedObject boundary honored — all in `tests/integration/phase3.test.js`
- [x] `docs/concepts/object.md` documents the object/mesh layer and the instancing decision table (Prompt 53)

**Phase 3 — DONE.**

---

### Phase 4 — Compositional Core — **DONE**
**Scope:** `src/compose/`  
**Prompts:** 41–55, 74–85 (Selection/join and Axis/annotation were pulled forward into Phase 4 as v3 additions — see `prompts.md`)  
**Exit criteria:**
- [x] Scales: `linear`, `log`, `pow`, `sqrt`, `band`, `point`, `ordinal`, `time` — all with `.domain()`, `.range()`, `.nice()`, `.clamp()`, `.ticks()`, `.tickFormat()` per D3 convention
- [x] Generators: `generator.line`, `generator.bar`, `generator.point`, `generator.surface`, `generator.arc` returning Three.js-ready buffers/geometry
- [x] Layouts: `force`, `tree`, `pack`, `stack`, `grid` — operate on plain data arrays, return position arrays
- [x] Color palettes: `viridis`, `plasma`, `inferno`, `magma`, `cividis`, `turbo` built-in; `color.sequential`, `color.diverging`, `color.categorical`; user-extensible via `palette.custom`/`fromCSS`
- [x] Axis: `Axis` renders tick lines, labels (SDF text after Phase 6), and a spine; consumes the scale's own `.ticks()`/`.tickFormat()`
- [x] Selection & the data join: `Selection`/`.data(newData, keyFn).join(...)` (v3 addition) — the flagship capstone (`docs/concepts/compose.md`, `examples/04-compose/`) proves Selection + join + scales + palettes compose without a chart class
- [x] All `compose/` modules are pure functions or classes with no Three.js import (Three.js objects are output only from generators) — except the three sanctioned carve-outs (`compose/selection`, `compose/axis`, `compose/annotation`), documented in `CLAUDE.md` §1.4
- [x] Coverage ≥ 85% lines, ≥ 80% branches across `src/compose/`

---

### Phase 5 — Animation & Transitions — **DONE**
**Scope:** `src/anim/`  
**Prompts:** 86–99 (renumbered from the original 56–65 as `prompts.md` grew during Phase 4's v3 additions — see `prompts.md`)  
**Exit criteria:**
- [x] `GraphAnimCurve` (Prompt 86): every in/out/inOut easing family, `spring`, `bezier`, `noise`, `resolve(nameOrFn)`
- [x] `GraphAnimKeyframe` (Prompt 87): per-property track with dot-paths; all interpolation delegates to `compose/interpolate` (no local lerp)
- [x] `GraphAnimTimeline` (Prompt 88): `to`/`from`/`wait`/`then`/`play`/`pause`/`stop`/`reverse`/`seek`/`loop`; parallel by default, sequential after `.then()`
- [x] `GraphAnim` (Prompt 89): engine root, one shared RAF tick drives every registered timeline; `timeline()`/`add()`/`remove()`, global `pause()`/`resume()`, `dispose()`
- [x] `Transition` (Prompt 90): D3-flavored `.duration()`/`.delay()`/`.easing()`/`.on('start'|'end'|'interrupt')`
- [x] `Selection.transition()` → `SelectionTransition` (Prompt 91): animated `.attr()`/`.style()`/`.remove()` over the join system, staggering via a per-datum `.delay()` function, batched per-frame buffer commits (never per-instance) on the instanced backend
- [x] `GraphInstancedObject.setAllPositions`/`setAllScales`/`setAllColors` (Prompt 92): optional `{duration, easing}` animates the whole bulk array instead of memcpy-ing it
- [x] Interrupt semantics (Prompt 93): a new `Transition`/`SelectionTransition` on the same target+path (or node+path) fires `'interrupt'` on the one it supersedes and picks up from the current interpolated state — `GraphAnimTimeline.interruptPath()` is the shared primitive underneath both
- [x] `CameraTour` (Prompt 94): waypoint interpolation (position + lookAt + FOV), per-segment duration/easing, `pause`/`resume`/`skipToNext`/`cancel`, presets `.orbit()`/`.flyTo()`/`.cinematicReveal()`
- [x] `GraphAnim.respectReducedMotion` (Prompt 95): registered timelines snap to their end values instead of animating; `GraphAnim.tween(from, to, options, onUpdate)` ad-hoc helper
- [x] Keyframe groups (Prompt 96): `GraphAnimTimeline.onGroupComplete()` — one completion event per `.then()`-delimited parallel group, independent of the timeline's overall `onComplete`; `Transition.runningOn(target)`/`cancelAllOn(target)` are the introspection primitive a future `chart.runningTransitions()`/`cancelTransitions()` (Phase 8) will wrap — no `src/chart/` layer exists yet to attach those to directly
- [x] `examples/05-transitions/main.js` (Prompt 97): a live data join re-transitioning every 2s (staggered enter, morphing update, dissolving exit) plus a `CameraTour.flyTo()` reframing on the new tallest bar
- [x] Phase 5 cross-cutting tests (Prompt 98, `tests/integration/phase5.test.js`): targets reached within tolerance; `.then()` sequencing; interrupt state pickup; reduced-motion snap; `SelectionTransition` parity meshes vs. instanced; per-datum stagger delay; exit `.remove()` frees instanced slots only after completion
- [x] `docs/concepts/anim.md` (Prompt 99): flat sugar API, timeline, and `SelectionTransition` documented side by side

---

### Phase 6 — Materials & Procedural FX — **DONE**
**Scope:** `src/material/`
**Prompts:** 100–115 (renumbered from this section's stale original range of 66–80 — corrected when the phase was completed, mirroring Phase 5's same correction)
**Exit criteria:**
- [x] `GraphObjectMaterial` (Prompt 100): `.set()`, `.applyShader()` (with dev-mode `preserveUniforms` hot-reload, Prompt 112), `.bindUniforms()` (`'auto'` time/resolution), `.setMap()` — all ref-counting-aware for shared textures (Prompt 111, `core/GraphDisposal.js`'s `retainTexture`/`releaseTexture`)
- [x] PBR pass-through presets (Prompt 101): `standard`, `physical`, `basic`, `lambert`, `phong`, `toon`, `matcap`
- [x] Custom-shader looks: `holographic` (Prompt 102), `crystal` (Prompt 103, chromatic-dispersion refraction), `glow` (Prompt 104)
- [x] `glass`/`frostedGlass` (Prompt 103, real `transmission` + thin-film `iridescence`)
- [x] `neon`/`pulse` (bloom-friendly emissive, breathing), `velvet` (Prompt 104)
- [x] Metals & coated dielectrics: `liquidMercury`, `chrome`, `gold`, `copper`, `pearl`, `obsidian` (Prompt 105)
- [x] `dataDriven` (Prompt 106): per-instance/per-vertex palette lookup; completes the Prompt 77 `Selection.style` per-instance `opacity`/`emissiveIntensity` link
- [x] The `material`/`texture` namespaces assembled and re-exported from `src/index.js` (Prompt 107)
- [x] SDF text (`SDFText.create`, Prompt 108): GPU-rendered, resolution-independent MSDF layout/shader engine — the bundled Roboto atlas binary itself is a known, documented gap (no MSDF tool/font available in this environment; same category as Phase 2's missing HDR assets)
- [x] Procedural textures (Prompt 110): `gradient`, `noise`, `voronoi`, `cellular`, `checkerboard`, `dots`, `lines`, `paletteTexture` — all `THREE.DataTexture`
- [x] `addPlanarReflection` (Prompt 111) and `setPaletteForAttribute` (Prompt 112) convenience helpers
- [x] `examples/06-materials/main.js` (Prompt 113): 4×4 grid, one preset per bar, `studio-dark` themed, verified live in a browser
- [x] Phase 6 cross-cutting tests (Prompt 114, `tests/integration/phase6.test.js`): every material renders/disposes clean, SDF crispness proxied structurally, palette texture matches its source fn, `dataDriven` samples correctly, `Selection.style('color')` backend parity
- [x] `docs/concepts/material.md` (Prompt 115) with a material-picker gallery
- [ ] No canvas-sprite text anywhere after this phase — not fully verifiable yet: `SDFText` exists and is the mandated replacement, but it isn't wired into `Axis`/`annotation.label` (still the Phase 4 metadata stub; see `docs/concepts/material.md`'s SDFText section for why) and no canvas-sprite text was ever added elsewhere, so there's nothing to replace today

---

### Phase 7 — PostFX & Particles — **DONE**
**Scope:** `src/postfx/`  
**Prompts:** 116–126 (renumbered from this section's stale original range of 81–95, mirroring Phase 5/6's same correction)  
**Exit criteria:**
- [x] `PostFX` (Prompt 116, not `GraphPostFX` as originally drafted — matches `prompts.md`'s literal class/file name) owns `EffectComposer`; `enable(name, opts)`/`disable(name)`/`configure(name, opts)`/`enabled()`; passes are automatically re-sorted into a registered `order`, independent of `enable()` call sequence; lazily instantiated on `graph3d.postfx` (a second sanctioned `Graph3D`-composition-root exception in `CLAUDE.md` §1.4, alongside `GraphScene`). Charts requesting passes via `graph3d.postfx.enable(passName, options)` is Phase 8's job — no chart type exists yet to do so.
- [x] Built-in passes (Prompt 117): `ssao` (`SSAOPass`), `bloom` (`UnrealBloomPass`), `dof` (`BokehPass`), `motionBlur` (custom camera-reprojection `Pass` — see scope note in `src/postfx/passes/motionBlur.js` and `skipping_list.md`), `colorGrading` (`LUTPass`, defaults to a generated neutral identity LUT), `vignette`/`chromaticAberration`/`filmGrain` (raw `three/addons/shaders/*` wrapped in `ShaderPass`/`FilmPass`), `fxaa` (`FXAAPass`), `smaa` (`SMAAPass`) — `godRays` is Prompt 118, not this one
- [x] `outline`/`godRays` (Prompt 118): `outline` wraps `OutlinePass` with a color-safe `configure` (`THREE.Color.set()` so hex numbers and `Color` instances both work) — forward-designed for Phase 9's hover/selection state machine to drive via `graph3d.postfx.configure('outline', { selectedObjects })`, since Phase 9 (`interact/`) doesn't exist yet and `postfx/` can't import from it either way (CLAUDE.md §1.4). `godRays` is a custom screen-space volumetric-light-scattering `Pass` (shares the `motionBlur`-established depth-prepass idiom, now extracted to `passes/_shared.js`'s `DepthPrepass` per DRY's two-strike rule) that auto-activates via `PostFX`'s constructor/`setSceneCamera` reading `scene.userData.graph3d_fogPreset === 'volumetric-cinematic'` (the flag `GraphSceneEnvironment.setFog` already stored for exactly this purpose) — see `skipping_list.md` for the screen-space-approximation scope note
- [x] `ssr`/presets (Prompt 119): `ssr` wraps `SSRPass` (backs Prompt 111's `material.addPlanarReflection({ ssrPass: true })` via its returned mirror as `groundReflector`); `PostFX`'s `enable()` gained a `canEnable(ctx)` gate so `ssr` can auto-disable itself with a `console.warn` on `CapabilityProbe`-reported weak GPUs (no WebGL2/float-textures — CLAUDE.md §1.5's capability-driven-fallback exception), plumbed end-to-end via `Graph3D`'s `postfx` getter passing `this.capabilities`. `PostFX.registerPreset`/`preset(name)` add 7 named look bundles (`cinematic`/`clean`/`dramatic`/`dreamy`/`editorial`/`cyberpunk`/`minimal`, `src/postfx/presets.js`) combining only the scene-agnostic stylistic passes — `godRays`/`outline`/`ssr` are excluded since they need scene-specific setup a generic preset can't assume (see `skipping_list.md`)
- [x] `ParticleSystem` (Prompt 120, not `GraphParticles` as originally drafted — matches `prompts.md`'s literal class/file name, `src/postfx/particles/ParticleSystem.js`): GPU-instanced ring-buffer particle pool, `emit({count, position, velocity, lifetime, size, color, blending})`, billboard (default) and mesh-particle rendering modes. Two simulation backends chosen once at construction from `CapabilityProbe` (`webgl2 && floatTextures` → GPU ping-ponged `WebGLRenderTarget` pair advanced by a `FullScreenQuad` shader pass; otherwise CPU-integrated `InstancedBufferAttribute`s, the iOS-Safari-safe fallback) — not literally "<10 000 CPU / ≥10 000 GPU" as originally drafted, since a capability-driven choice (this prompt's own explicit requirement) is more correct than a hardcoded count threshold. No behaviors (gravity/wind/etc.), `spawnAt(mesh)`, or presets yet — Prompt 121's job. Integrating with the animation loop is the caller's responsibility (`g.loop.add((dt) => system.update(dt))`) — this class never schedules its own RAF, per CLAUDE.md §2, and no prompt asks for a `Graph3D.particles` accessor
- [x] Particle behaviors + presets + `spawnAt` (Prompt 121): `addBehavior`/`removeBehavior`/`configureBehavior` for `gravity`/`wind`/`attract`/`repel`/`curl`/`swirl` (`src/postfx/particles/behaviors.js` for the CPU math, `behaviorShaders.js` for the GLSL — same formulas, ported by hand since a fragment shader can't `import` JS, a documented cross-language DRY exception). Adding a behavior on the GPU path required redesigning Prompt 120's single-texture "static velocity" GPU sim into a proper two-pass integrator: velocity+lifetime is now its own ping-ponged `WebGLRenderTarget` pair (was a static `DataTexture`), updated first from the active behaviors' accelerations, then position integrates from that fresh velocity — the velocity pass's shader is rebuilt (not just re-uniformed) whenever the active behavior *set* changes. `spawnAt(source, options)` (`meshSampling.js`) samples `options.count` points off a mesh's surface (area-weighted per-triangle sampling), defaulting velocity to outward-along-normal — accepts a raw `THREE.Mesh` or anything exposing one as `.three` (duck-typed, so `postfx/` never imports `object/`). Six presets (`ParticleSystem.registerPreset`/`preset(name)`, mirroring `PostFX.registerPreset`, `src/postfx/particles/presets.js`): `dust`, `sparks`, `smoke`, `confetti`, `dataStream`, `dissolve` (the last delegates to `spawnAt` when given a `mesh` option, otherwise a point burst) — none animate opacity over lifetime, since the render shader only supports a hard discard at death (see `skipping_list.md`)
- [x] No chart type creates its own `EffectComposer` — vacuously true today (no chart type exists yet, Phase 8), and `PostFX` is the only class that constructs one
- [x] Disposal test: all passes release GPU resources (`tests/postfx/passes.test.js`, `tests/postfx/PostFX.test.js`); `ParticleSystem` disposal covered by `tests/postfx/particles/ParticleSystem.test.js` (CPU path fully — including behaviors, `spawnAt`, and presets — GPU path construction/dispose/mode-selection only — see `skipping_list.md`)
- [x] `dissolve` wired as an animated exit (Prompt 122): `Selection.remove(animationName, options)` (`src/compose/selection/Selection.js`) plays `options.system.preset(animationName, { mesh|position, ...opts })` at each departing node's location before freeing it — `options.system` is duck-typed (any object exposing `.preset(name, opts)`, i.e. a `ParticleSystem`) since `compose/` must not import `postfx/` and `Selection` has no scene/camera/renderer to build one itself. `chart.exitAnimation(name, options)` — the other half, deferred at the time since `GraphChart` didn't exist until Prompt 127 — was wired in a post-Phase-9 skipping_list.md sweep: `GraphChart.exitAnimation()` stores the config, `update()`'s exit-join calls the same `Selection.remove` path directly (no second implementation) whenever it's set and no `on('exit', fn)` handler is registered
- [x] `PostFX.pipeline(order)` (Prompt 123): full manual override of the composer's pass render order, superseding the registered `order`-field auto-sort that `enable()`/`disable()` normally maintain. Takes every currently-enabled pass name exactly once in the desired sequence (or `null` to clear the override and resume auto-sorting) — throws on an unknown/duplicate/missing name. The override is a live filter, not a frozen snapshot: a pass named in it that's later `disable()`d is simply skipped, and a *new* pass `enable()`d afterward that wasn't named in it is appended at the end (auto-sorted among such newcomers) rather than silently dropped from the chain. `preset()` clears any active override, matching its own documented "deterministic bundle, not a merge" contract
- [x] `examples/07-postfx/main.js` (Prompt 124): a torus-knot gallery (standard/neon/glow materials, staggered depth) with a preset toggle bar cycling Prompt 119's 7 named `PostFX` presets and a "100K-particle rain" button lazily constructing one large-capacity `ParticleSystem` and bursting 100,000 falling particles per click. Verified live in a browser (Chrome via `vite examples/07-postfx`) — surfaced and fixed a genuine pre-existing bug in the process: `particleShaders.js`'s `POSITION_LOOKUP_GPU`/`_CPU` spliced `attribute`/`uniform` declarations *inside* `void main() { ... }`, invalid GLSL that only a real WebGL context could catch (jsdom never compiles a real shader — exactly the gap `skipping_list.md` already flagged). Split each into `declarations` (placed at global scope) and `body` (placed inside `main()`); confirmed the fix via the browser's shader-compile console output going clean. The example's canvas rendering couldn't be fully re-verified after that fix — the automated browser tab landed in a backgrounded (`document.hidden`) state partway through testing, which throttles Chrome's render loop independent of any Graph3D code (confirmed via `document.visibilityState`/`hasFocus()` and a zero-rAF-callbacks probe) — but the gallery render, preset-driven bloom, and error-free emit were all confirmed working before that
- [x] Phase 7 cross-cutting tests (Prompt 125, `tests/integration/phase7.test.js`): (a) every built-in pass enables/disables/disposes cleanly together, including a 100×-repeated all-13-passes leak smoke test and `pipeline()`-reordering-then-disable combinations; (b) preset combinations — cycling all 7 `PostFX` presets on one instance leaks no pass from the previous preset, all 6 `ParticleSystem` presets apply in sequence on one system without throwing, each preset stays independent on its own fresh system, and applying two presets that both configure `wind` on the *same* system reconfigures rather than throwing (confirms the Prompt 121 skipping_list.md-documented behavior-key-collision semantics, doesn't newly discover it); (c) "100K particles at 60fps" is proxied by timing the **CPU** sim path (jsdom has no real WebGL to time the GPU path or a real frame rate) — emit 100,000 particles + 60 simulated `update()` frames asserted well under a 5s budget — with the literal real-time/real-browser claim instead checked by hand via Prompt 124's example, which is also where a real GPU shader bug got caught; (d) CPU-vs-GPU numerical parity remains explicitly untested (same jsdom-has-no-real-WebGL blocker) — logged, not faked, in `skipping_list.md`
- [x] `docs/concepts/postfx.md` (Prompt 126) with a visual gallery: `PostFX` (enable/disable/configure, all 13 built-in passes with `order`/options, `godRays` auto-activation, the 7 presets, `pipeline()`), `ParticleSystem` (GPU/CPU backends, the 6 behaviors, `spawnAt`, the 6 particle presets), the Prompt 122 `Selection.remove(animationName, options)` exit-animation wiring (and its deferred `chart.exitAnimation` half), and a gallery table for `examples/07-postfx/main.js` — including the real GLSL bug that building and browser-testing that example caught, which no jsdom unit test could have

---

### Phase 8 — Chart Types
**Scope:** `src/chart/`  
**Prompts:** 96–140  
**Exit criteria:**
- [x] `GraphChart` base class (Prompt 127, `src/chart/GraphChart.js`): fluent config state — `data(arr, keyFn)`, `x/y/z(accessorOrScale, scale)` (accessor + optional `compose/scale`, defaulting to index/identity/constant-0), `color(accessorOrConstant, palette)`, `size`/`shape` accessors, `material(presetName, options)` (validated against the real `material` preset names, excluding its two non-preset utility exports), `filter`/`sort`, `transition(durationMs, easingNameOrFn)` (validated eagerly via `anim`'s `resolve`, mirroring `Transition.easing()`), and `on('enter'|'update'|'exit', handler)` — every setter chainable, every input validated at the boundary (Fail Fast). `render()`/`update()`/`destroy()` were documented throw-stubs at the time (mirroring `Selection.on()`'s identical Phase-9-pending pattern) — their real bodies landed in Prompts 129/130/131 respectively, not this one
- [x] Join-native chart surface (Prompt 128): `chart.data(arr, keyFn)` now delegates to an internally-owned `Selection` (`compose/selection`'s Phase 4 join machinery, `computeJoin`/`JoinResult` — no reimplementation, CLAUDE.md §1.1 DRY) and returns the resulting `JoinResult` (`.enter()/.exit()/.join(enterFn, updateFn, exitFn)`), not `this`. That internal `Selection` starts wrapping an empty `{ type: 'meshes', meshes: [] }` backend — valid and zero-cost pre-render since an empty array vacuously satisfies the meshes shape; calling `.enter()` on it with real entering data before `render()` (Prompt 129) has attached a real backend throws the existing, correct `materializeEnter` "requires a mesh template" error rather than faking success. `chart.onEnter(fn)`/`onUpdate(fn)`/`onExit(fn)` are sugar over Prompt 127's `on('enter'|'update'|'exit', fn)`, and `chart.selection()` returns that same live backend `Selection` directly for post-render micro-control. Prompt 129/130 will replace `#backendSelection` with a real one once `render()`/`update()` exist — deliberately not built ahead of that here (YAGNI)
- [x] `render()` first-call materialization (Prompt 129): applies `filter`/`sort` (if set) to the last array passed to `data()`, fits every scaled `x`/`y`/`z` field's domain to the result via that field's own accessor (`[min, max]` for continuous scales — anything exposing `.invert` — or the raw per-datum values for ordinal-like scales, whose own `domain()` setter already dedupes), wires the resolved `accessor ∘ scale` functions into the generator's own `x`/`y`/`z` setters (only the ones it exposes — `generator.bar()` has no `z`), computes buffers via `generator.compute(data)`, and materializes them through `GraphObjectFactory` — `createPoints` (sphere) for a `.shape()`-exposing generator (`generator.point()`), `createBars` (box) otherwise — which itself picks a `GraphMesh[]` or a `GraphInstancedObject` per `INSTANCING_THRESHOLD` (CLAUDE.md §1.1 DRY: that dispatch already lives there). The resolved material comes from `material[presetName](options)` if `.material()` was configured, else `material.standard()`. `#backendSelection` is replaced with a `Selection` over the real backend, so `data()`/`selection()` reflect it from this point on. Every later `render()` call routes to `update()` instead (Prompt 130). Color/size/shape config (Prompt 127) remain unconsumed for now — no current prompt assigns them a materialization path yet. The meshes-backend `Selection` also carries a `template` (`{scene, name, geometry, material}`, reusing `backend[0]`'s own cloned geometry/material) so a future `update()` can materialize newly-entering meshes without one
- [x] `update()` diff-based sync (Prompt 130): joins the latest `data()` array against what's currently bound via `diffData` (`compose/selection/diff.js`, now re-exported through `compose/selection/index.js`/`compose/index.js` specifically because its own header already anticipated this exact consumer — CLAUDE.md §1.1 DRY, no reimplemented diffing in `chart/`), diffing twice (once directly for precise `newIndex`/`oldIndex` metadata, once more inside `Selection.data()`'s own join) — an accepted, bounded 2x cost of never reaching past `Selection`'s public surface into its private backend shape. For each of the update/enter/exit groups: if the matching `on('enter'|'update'|'exit', fn)` handler(s) are registered, they run *instead of* any default (mirrors `JoinResult.join()`'s established "handler XOR default" semantics — Prompt 130's own "user's join fns **or** defaults" wording); otherwise the default applies — update/enter write `generator`-recomputed position/scale via `Selection.attr()` (immediately) or `SelectionTransition.attr()` (animated, if `.transition()` is configured — "respects active transitions"), and exit shrinks `scale` to 0 and fades `opacity` to 0 (a material-independent "dissolve" — a pure opacity fade has no visual effect on the instanced backend without the Phase 6 `dataDriven` material) before `.remove()`, using the chart's configured duration/easing or `SelectionTransition`'s own built-in default (250ms/linear). Every write is skipped entirely for an empty group (no dangling `SelectionTransition` registered on the shared `anim` engine for nothing). `#backendSelection` becomes `joined.merge(entered)` — the live set going forward
- [x] `destroy()` full disposal (Prompt 131): stops every `SelectionTransition` `update()`'s own default (non-handler) writes started and hasn't finished yet — a new public `SelectionTransition.stop()` unregisters its internal timeline from the shared `anim` engine, abandoning pending writes rather than letting them run against a chart that's going away — then disposes the live backend via a new public `Selection.dispose()` (disposes every `GraphMesh`, or the one shared `GraphInstancedObject`, per backend type; a sanctioned `compose/selection` → `object/` reach, same as the rest of the file). Departing (exiting) members are excluded from `#backendSelection` by `update()`'s own `joined.merge(entered)` once `.exit()` is called, so a dissolve still mid-flight at `destroy()` time would otherwise leak — tracked separately (`#pendingExits`) and force-disposed too. Idempotent (`#destroyed` guard); every other public method now asserts not-disposed and throws `GraphChart.<method>: this chart has been destroyed` afterward (CLAUDE.md's Disposal Contract), mirroring `GraphObject`/`GraphMesh`'s existing per-method guard pattern. Does not dispose any axis/annotation — no current prompt attaches either to a `GraphChart` instance, so there is nothing of that kind to release yet. 1000×-cycle leak test added (`tests/integration/GraphChart-disposal.test.js`) for both the meshes and instanced backends
- [x] `BarChart` (Prompt 132, `src/chart/BarChart.js`): wraps `generator.bar()`, defaulting `.material('standard')` and `.transition(800)` — everything else (`INSTANCING_THRESHOLD`-driven backend choice, `material.standard()` fallback) is already `GraphChart`'s own default, inherited unchanged. `.grouped(keyFn)`/`.stacked(keyFn)` set a mutually-exclusive series mode consumed by a wrapped `compute(data)` (the real `generator.bar().compute`, captured once and closed over before being reassigned — `GraphChart` only stores the generator reference, so mutating `bar.compute` after `super()` still reaches it): `.grouped()` narrows and offsets each bar to `originalExtent / seriesCount` along `x` (or `z` if `.depthSeries()` is active); `.stacked()` pivots data into per-category series via `layout.stack()` (CLAUDE.md §1.1 DRY — the same stacking math `layout.stack`'s own header already anticipated this exact consumer) and overrides the bar generator's `y`/`baseline` per datum to the resulting `[y0, y1]` band, restoring the prior accessor afterward. `.horizontal()`/`.vertical()` swap the `x`/`y` components of the computed position/scale buffers post-generator. `BarChart` is also the first (and so far only) place `GraphChart`'s `#colorField` (Prompt 127) actually gets consumed: `render()`/`update()` overrides call `super` then, if `.color()` has an accessor configured, write per-datum colors via `Selection.attr('color', ...)`, falling back to `palette.viridis` when no palette was given (`color.sequential` fit to the current data's `[min, max]`) — color writes are immediate, not routed through the configured transition (no current prompt asks for animated color). Unit tests (`tests/chart/BarChart.test.js`), an integration test covering both backends (`tests/integration/BarChart.test.js`), a live example (`examples/08-bar-chart/`), and `docs/concepts/chart.md` (new — covers `GraphChart` + `BarChart` together, since neither had a docs page yet)
- [x] `LineChart` (Prompt 133, `src/chart/LineChart.js`): wraps `generator.line()`, rendering one continuous `GraphLine` (a `Line2`, new `object/GraphLine.js` — the one chart primitive `GraphObjectFactory` has no factory for, since every existing one is N independent instances, not one continuous path) per series instead of one mesh/instance per datum. `GraphChart.render()`/`update()` assume a per-datum position+scale buffer materialized via `GraphObjectFactory.createBars`/`createPoints`, which doesn't fit a continuous path, so `LineChart` overrides `render()`/`update()`/`destroy()`/`data()` entirely rather than building on them — it does still reuse `GraphChart`'s inherited `x()`/`y()`/`z()` field storage, and the axis scale-domain fitting those fields need, extracted out of `GraphChart`'s own (private, unreachable-by-subclass) `#applyScaleDomain`/`#resolvedAccessor` into a new shared `chart/axisField.js` (CLAUDE.md §1.1 DRY two-strike rule — second consumer). `.series(keyFn)` splits `data()` into independently-tracked lines, one `GraphLine` per distinct key, colored via `palette.category10` (auto-assigned per key in first-seen order); without it, all of `data()` renders as one line. `.curve(type)` passes straight through to `generator.line().curve()` (no second curve table). `GraphLine.setPositions()` mutates the existing `Line2` geometry's interleaved buffer in place when the point count matches the previous call ("same-count updates mutate vertices in place" — the prompt's own wording) and only reallocates when it changes. `data(arr)` is a plain getter/setter here (not `GraphChart`'s join-native version) — a continuous polyline has no per-vertex `Selection` backend to `.enter()`/`.exit()` one point at a time, so `selection()`/`on(...)` (inherited from `GraphChart`) are inert for `LineChart`, documented explicitly rather than silently overridden. `.setResolution(width, height)` updates every live line's `LineMaterial.resolution` uniform (`linewidth` is measured in screen pixels; not wired to `window.resize` automatically). Unit tests (`tests/chart/LineChart.test.js`, `tests/object/GraphLine.test.js`), an integration test (`tests/integration/LineChart.test.js`), a live example (`examples/09-line-chart/`), and `docs/concepts/chart.md` updated
- [x] `ScatterChart` (Prompt 134, `src/chart/ScatterChart.js`): wraps `generator.point()` — every `GraphChart` default (instanced-over-50-datums, `material.standard()` fallback) already applies unchanged, so a million-point scatter plot renders as one `GraphInstancedObject` for free. `.size(fn)` wires into `generator.point().size(...)` before `compute()` via the same "wrap `compute`, capture the raw fn before reassigning" mechanism `BarChart` established — the first time `.size()` (Prompt 127) actually gets consumed, since `GraphChart.#computeBuffers` only ever wires `x`/`y`/`z` into the generator. `.color(fn, palette)` reuses `BarChart`'s `palette.viridis`-fallback write, extracted into a new shared `chart/colorField.js` once `ScatterChart` became its second consumer (CLAUDE.md §1.1 DRY two-strike rule) — `BarChart.js` refactored to use it too, verified behavior-preserving against its existing test suite. `.opacity(valueOrFn)` is new (a constant or per-datum accessor via `Selection.attr('opacity', ...)`) — no other current chart type needs it, so it lives on `ScatterChart` only, not `GraphChart`. `.pick(raycaster)` returns the hit datum (or `null`): on the instanced backend it delegates straight to `GraphInstancedObject.pick()` (Prompt 45's already octree-backed picking — no second, redundant spatial index built here), on the meshes backend (≤50 points) a plain `THREE.Raycaster.intersectObjects` (octree overhead isn't worth it at that scale). Reaching the live backend needed one new addition to `compose/selection/Selection.js`: a read-only `get backend()` escape hatch (mirrors `GraphObject`'s own `get three()`) — `Selection` previously had no way to hand out its underlying `GraphMesh[]`/`GraphInstancedObject`, and this doesn't touch the still-unimplemented `Selection.on(event, handler)` Phase-9 stub, which remains exactly as it was. Unit tests (`tests/chart/ScatterChart.test.js`), an integration test covering both backends including picking (`tests/integration/ScatterChart.test.js`), a live example (`examples/10-scatter-chart/`, 5,000 instanced points with octree-backed hover picking), and `docs/concepts/chart.md` updated
- [x] `AreaChart` + `SurfaceChart` (Prompt 135, `src/chart/AreaChart.js` + `src/chart/SurfaceChart.js`): both are triangulated-mesh chart types, not per-datum position+scale ones, so both override `render()`/`update()`/`destroy()` themselves (mirroring `LineChart`'s precedent) rather than building on `GraphChart`'s own. `AreaChart` wraps a new `generator.area()` — shares `generator.line()`'s `x`/`y`/`z`/`curve`/`tension` fields and top-edge curve sampling (CLAUDE.md §1.1 DRY, no reimplemented curve math), extruding the sampled top edge down to a constant `.baseline()` into a triangulated vertical "wall" (`{positions, indices, normals}`, the same shape `generator.surface()`/`generator.arc()` already return). `SurfaceChart` wraps the existing `generator.surface()` directly — no per-datum concept applies to a heightfield, so `GraphChart`'s `x()`/`y()`/`z()`/`data()`/`color()`/`size()`/`shape()`/`filter()`/`sort()`/`on()`/`selection()` are all inert for it (documented explicitly, same precedent as `LineChart`'s inert `selection()`/`on()`); `.values()`/`.xDomain()`/`.zDomain()`/`.resolution()` instead mirror the generator's own chainable API 1:1. Both materialize via a new `GraphObjectFactory.createTriangleMesh(name, {scene, positions, indices, normals, material})` — the one factory method there producing a single continuous `GraphMesh` from a generator's raw triangulated-mesh output, rather than N *instanced* primitives like every other factory method there. Both reuse `GraphChart`'s `.material()` field via a newly-extracted `chart/materialField.js` (`GraphChart`'s own private `#resolveMaterial()`, now shared — CLAUDE.md §1.1 DRY two-strike rule, `GraphChart`'s test suite reverified behavior-preserving after the extraction). Neither mutates its mesh in place on `update()` (unlike `LineChart`'s `GraphLine`) — disposes and rebuilds from fresh values every call instead, since no current requirement calls for the optimization and CLAUDE.md §1.3 YAGNI says profile before optimizing. `SurfaceChart.contours(levels)` optionally overlays isolines at given height values via a new marching-squares tracer (`compose/generator/contour.js`), operating directly on `generator.surface().compute()`'s own already-computed `positions` grid (that `compute()` now additionally returns `rows`/`cols` alongside its existing `positions`/`indices`/`normals`, a small additive change, verified non-breaking against `generator.surface()`'s existing test suite) rather than re-sampling the heightfield a second time. Each traced path (open or a closed loop; one level can trace several disjoint paths) is stitched from per-grid-cell segments via shared canonical edge keys (so two adjacent cells' independently-computed shared crossing always matches, no floating-point fuzzing needed) and rendered as its own `GraphLine` (Prompt 133's wrapper, reused as-is, a constant black). The ambiguous 4-edge-crossing "saddle" cell case is resolved via one deterministic rule (documented, not a full asymptotic decider) — a first-pass simplification, not a geo-scientific-grade contour algorithm. Unit tests (`tests/compose/generator/area.test.js`, `tests/compose/generator/contour.test.js` — including a hand-traced 2-cell stitching case verifying exact interpolated coordinates, `tests/chart/AreaChart.test.js`, `tests/chart/SurfaceChart.test.js`, `tests/object/GraphObjectFactory.test.js` additions), integration tests (`tests/integration/AreaChart.test.js`, `tests/integration/SurfaceChart.test.js`), live examples (`examples/11-area-chart/`, `examples/12-surface-chart/`), and `docs/concepts/chart.md` updated
- [x] `HeatmapChart` (Prompt 136, `src/chart/HeatmapChart.js`): wraps a new `generator.heatmap()` — a fixed-size grid-cell box, not a baseline-relative growth shape like `generator.bar()` (no baseline concept applies to a heatmap cell). Every `GraphChart` default (instanced-over-50-datums, `material.standard()` fallback) already applies unchanged, so a million-cell heatmap renders as one `GraphInstancedObject` for free — `GraphObjectFactory.createBars`'s existing box geometry (unmodified) already covers both this chart's render paths (2D plane tiles and 3D voxel cubes are both just boxes at different heights), so no new factory method was needed. `.mode(name)` (`'plane'`, default, or `'voxel'`) only changes the computed cell height (`generator.heatmap().height()` — thin constant for `'plane'`, equal to `.width()` for `'voxel'`, a real cube) before delegating to the generator's own `compute`, mirroring `BarChart`/`ScatterChart`'s "wrap compute, capture the raw fn before reassigning" mechanism; `x`/`y`/`z` position is unaffected by mode either way. The constructor defaults `.y(0)` so `'plane'` mode's tiles lie flat without the caller configuring anything (`GraphChart`'s own default `y` accessor — the whole datum — would otherwise produce `NaN` positions). `.color(fn)` is `HeatmapChart`'s third use of `chart/colorField.js` (same `palette.viridis` fallback as `BarChart`/`ScatterChart`). `.opacity(fn)` becomes `HeatmapChart`'s second use of a value first introduced by `ScatterChart` (Prompt 134) — since that made `HeatmapChart` its second consumer, `.opacity()`'s storage/getter/setter moved off `ScatterChart` (where it was originally private) and onto `GraphChart` itself, mirroring `.size()`/`.shape()` (CLAUDE.md §1.1 DRY two-strike rule); the actual write (`chart.selection().attr('opacity', ...)`) is now a shared `chart/opacityField.js`, and `ScatterChart` was refactored to use both, reverified behavior-preserving against its existing test suite. Unit tests (`tests/compose/generator/heatmap.test.js`, `tests/chart/HeatmapChart.test.js`), an integration test covering both modes and the instanced backend (`tests/integration/HeatmapChart.test.js`), a live example (`examples/13-heatmap-chart/`, an animated 24×24 grid with a plane/voxel toggle), and `docs/concepts/chart.md` updated
- [x] `NetworkChart` (Prompt 137, `src/chart/NetworkChart.js`): node positions come from a live `layout.force()` simulation, not an accessor+scale computation, so — like `LineChart`/`SurfaceChart` — it overrides `data()`/`render()`/`update()`/`destroy()` entirely rather than building on `GraphChart`'s per-datum pipeline (its `super()` generator argument is an inert `{compute: () => ({})}` stub, since there's no `compose/generator` concept that fits: nothing computes buffers from an accessor pipeline here). Nodes render as spheres via the already-present `GraphObjectFactory.createNodes` (instanced above `INSTANCING_THRESHOLD`, same as every other chart); edges render as one `GraphLine` (`Line2`) per link — reused as-is, the same primitive `LineChart`/`SurfaceChart`'s contour overlay already established for continuous paths, rather than an instanced N-datum path. `.data(nodes)` is a plain getter/setter (like `LineChart.data()`) with identity by object reference — passing the same node objects across `update()` calls preserves their simulated position/velocity, since `layout.force().nodes()` only seeds missing `x`/`y`/`z`/`vx`/`vy`/`vz` fields, not overwrite existing ones. `.links(links)` (`{source, target}` pairs, index or node reference) and `.linkDistance(value)` pass straight through to `layout.force.link`'s own `distance` option (CLAUDE.md §1.1 DRY). `.cluster(keyFn)` is backed by a new `layout.force.cluster` factory (`compose/layout/force/forces.js`) — a sixth force alongside `.link`/`.charge`/`.center`/`.collide`/`.radial`, pulling nodes sharing `keyFn`'s value toward their shared centroid; added as a proper force factory rather than a chart-local closure because the accumulator fields it writes (`node.__ax`/`__ay`/`__az`) are `layout.force`'s own internal contract, not something `chart/` should reach into directly. `.pin(node, position?)`/`.unpin(node)` set/clear `fx`/`fy`/`fz` (which `layout.force()`'s own `tick()` already special-cases) and call `.restart()` to wake an auto-paused simulation. `.tick()` is the externally-driven step (CLAUDE.md §2: no internal `requestAnimationFrame` — call it from your own `loop.add(cb)`); it's a no-op once `layout.force()`'s own `alpha` decays below `alphaMin`, which *is* Prompt 137's "auto-pause on stability" (not reimplemented — `layout.force()` already auto-pauses; the chart just surfaces its `active()` check). `.selection()` is overridden to expose a real `Selection` over the node backend (rebuilt each `render()`/`update()`, mirroring `SurfaceChart`'s full-rebuild-per-update approach since a fixed-capacity `GraphInstancedObject` can't grow/shrink in place) so `.color()` still works via the same `chart/colorField.js`/`chart/materialField.js` helpers every other chart type uses — `GraphChart`'s inherited `x()`/`y()`/`z()`/`size()`/`shape()`/`opacity()`/`filter()`/`sort()`/`on()` are inert (documented explicitly, same precedent as `SurfaceChart`'s inert fields), since position/membership come from the simulation and `.links()` instead. Unit tests (`tests/compose/layout/force/forces.test.js` additions for `forceCluster`, `tests/chart/NetworkChart.test.js`), an integration test covering both backends, `.tick()`-to-stability, `.pin()`, and `.cluster()` (`tests/integration/NetworkChart.test.js`), a live example (`examples/14-network-chart/`, a growable force-directed graph with cluster toggle and a live settling/stable indicator), and `docs/concepts/chart.md` updated
- [x] `TreeChart` + `PackChart` (Prompt 138, `src/chart/TreeChart.js` + `src/chart/PackChart.js`): both render a single-root hierarchy via the pre-existing `layout.tree()`/`layout.pack()` (Prompt 73) instead of a flat data array, so — like `NetworkChart` — both override `data()`/`render()`/`update()`/`destroy()` entirely rather than building on `GraphChart`'s per-datum pipeline (same inert `{compute: () => ({})}` `super()` stub `NetworkChart` established, since no `compose/generator` concept fits a one-shot hierarchy layout). `.data(rootDatum)` takes one root object, not an array. `.children(fn)`/`.value(fn)`/`.sortChildren(fn)` forward straight to the underlying layout's own `children`/`value`/`sort` options (CLAUDE.md §1.1 DRY — no second hierarchy-walk in `chart/`); `sortChildren` is deliberately named apart from the inherited (and, here, inert) `GraphChart.sort()`, since it orders sibling nodes within the hierarchy, not a flat array. Every hierarchy node (root, internal, and leaf) renders as a sphere via the existing `GraphObjectFactory.createNodes`, sized by its own `.r` through a new shared `chart/hierarchyField.js` (`flattenHierarchyNodes` — walks a positioned root into a flat render array, the second-consumer DRY extraction both chart types needed; `nodeScaleForRadius` — converts a node's world-unit `.r` into the uniform instance/mesh scale factor `createNodes`' default sphere geometry needs to render at that exact radius, single-sourcing the `0.2` base-radius constant rather than duplicating it per chart). `.color()`'s accessor receives each hierarchy node itself (not the raw datum), so `(d) => d.depth`/`(d) => d.value`/`(d) => d.data.someField` all work — `GraphChart`'s inherited `x()`/`y()`/`z()`/`size()`/`shape()`/`opacity()`/`filter()`/`sort()`/`on()` are inert (documented explicitly, same precedent as `NetworkChart`'s inert fields). `TreeChart` additionally fans nodes radially by depth (`layout.tree()`'s existing "conical tree" layout) via new `.levelHeight(n)`/`.levelRadius(n)` passthroughs, and renders one `GraphLine` (`Line2`) per parent-child edge — the same primitive `NetworkChart`'s edges already established, walked via a `TreeChart`-private `#flattenLinks` (only one consumer, so not extracted to `hierarchyField.js`). `PackChart` instead nests value-sized, non-overlapping spheres via `layout.pack()`'s existing sphere-packing layout — the 3D analogue of d3.pack's nested circles — via a new `.padding(n)` passthrough; there are no edges, since nesting itself conveys structure. Both fully rebuild their node (and, for `TreeChart`, edge) backend on every `update()` — same full-rebuild-per-update tradeoff `NetworkChart`/`SurfaceChart` already accepted, not incremental (documented in `skipping_list.md`). Unit tests (`tests/chart/hierarchyField.test.js`, `tests/chart/TreeChart.test.js`, `tests/chart/PackChart.test.js`), integration tests covering both backends, hierarchy rebuilds, and non-overlap of packed sibling spheres (`tests/integration/TreeChart.test.js`, `tests/integration/PackChart.test.js`), live examples (`examples/15-tree-chart/`, a regenerable radial org-chart-style tree colored by depth; `examples/16-pack-chart/`, a regenerable nested-sphere hierarchy with a padding toggle and translucent material so nesting reads from outside — a 3D pack chart's opaque root otherwise fully occludes its own children, unlike 2D d3.pack's flat circles), and `docs/concepts/chart.md` updated
- [x] `PieChart` + `VolumeChart` (Prompt 139, `src/chart/PieChart.js` + `src/chart/VolumeChart.js`): `PieChart` renders a proportional-sweep pie/donut via a new `layout.pie()` (`src/compose/layout/pie.js` — d3-shape parity, chainable-only like `layout.stack()`, not an options-object constructor like `layout.tree()`/`layout.pack()`: `.value(fn).sort(fn).padAngle(n)`) extruded into wedges by the pre-existing `generator.arc()`, called once per slice (not once for the whole array) since every slice's wedge shape genuinely differs — each slice is therefore its own `GraphMesh` (`GraphObjectFactory.createTriangleMesh`), not instanced, unlike `BarChart`/`ScatterChart`. Like `NetworkChart`/`TreeChart`/`PackChart`, it overrides `data()`/`render()`/`update()`/`destroy()` entirely; `.innerRadius(n)`/`.outerRadius(n)`/`.extrude(n)` forward to `generator.arc()`'s own options, `.sortSlices(fn)` is named apart from the inherited (inert) `GraphChart.sort()` (same precedent as `TreeChart`/`PackChart`'s `.sortChildren()`). "Explode-on-hover" isn't owned by the chart (no `interact/` layer yet) — `.pick(raycaster)`/`.explode(datum, exploded?)` mirror `ScatterChart.pick()`'s established pattern, with the caller wiring its own `pointermove`+raycaster (`examples/17-pie-chart/`). Building this surfaced a real, previously-shipped bug: `chart/colorField.js`'s `applyColorField` always routed a chart's `.color()` palette through `color.sequential`'s `[min, max]` domain-fitting, which silently broke every chart's documented `palette.category10` categorical-coloring examples (including `NetworkChart`'s own class doc) — a categorical palette is already a complete key→color mapping (the same way `LineChart.series()` already calls `palette.category10(key)` directly) and doesn't want numeric domain-fitting at all. Fixed by tagging categorical palettes with `.categorical = true` (`compose/palette/categorical.js`) and having `applyColorField` call them directly when set. `VolumeChart` renders a ray-marched scalar field: `.values(fn)` (a `(x,y,z) => number` sampling function, mirroring `SurfaceChart`'s `.values((x,z) => number)`) is sampled onto a `.resolution()³` grid, normalized to `[0,1]`, and uploaded as a `THREE.Data3DTexture` consumed by a new `material.volumeRaymarch` (`material/presets/volumeRaymarch.js`, a `THREE.ShaderMaterial` requiring WebGL2/GLSL3 for `sampler3D`) that marches each view ray through a unit cube — built via `GraphObjectFactory.createTriangleMesh` with a hand-authored cube buffer rather than `GraphObjectFactory.createBars`, since `createBars`/`createMesh` clone whatever material they're given (would silently orphan the shader's own owned density/palette textures behind a disposed-but-inert clone). `.steps(n)` (ray-march step count, a direct quality/perf knob), `.densityScale(n)`, and `.palette(fn)` (defaults to `palette.viridis`) configure the shader; `.opacity(n)` overrides `GraphChart`'s inherited per-datum accessor with a plain global-alpha number (no per-datum concept applies to one continuous volume) — `GraphChart`'s other inherited fields, including `.material()` (this chart's rendering *is* `material.volumeRaymarch(...)`, always), are inert. Building the shader hit two real GLSL compile errors only caught by loading `examples/18-volume-chart/` in an actual browser (this project's jsdom test environment can't compile shaders — 17 structural unit tests all passed throughout): `gl_FragColor` (removed in GLSL ES 300, needs a declared `out vec4`) and an undeclared `modelMatrix` uniform (Three.js auto-injects `cameraPosition`/`modelViewMatrix`/`projectionMatrix` into a bare `ShaderMaterial` but not `modelMatrix` alone) — plus a raymarching-classic precision bug (the ray's start position sits exactly on the cube boundary, and GPU perspective-correct varying interpolation isn't bit-exact, so the first bounds check could fail before a single sample; fixed with a small boundary epsilon). Unit tests (`tests/compose/layout/pie.test.js`, `tests/chart/PieChart.test.js`, `tests/chart/VolumeChart.test.js`, `tests/chart/colorField.test.js`, `tests/material/presets/volumeRaymarch.test.js`), integration tests (`tests/integration/PieChart.test.js`, `tests/integration/VolumeChart.test.js`), live examples (`examples/17-pie-chart/`, an explode-on-hover donut/pie toggle; `examples/18-volume-chart/`, a switchable Gaussian-blobs/torus field with a steps quality toggle), and `docs/concepts/chart.md` updated
- [x] `Graph3D.chart(typeName)` dispatch (Prompt 140, `src/core/Graph3D.js`): registers all 11 concrete chart classes (`'bar'`/`'line'`/`'scatter'`/`'area'`/`'surface'`/`'heatmap'`/`'network'`/`'tree'`/`'pack'`/`'pie'`/`'volume'`) on the pre-existing private static `#chartTypes` registry — a stub from an earlier prompt that threw "none registered yet" for every call, since nothing had been wired in. Fixed two real bugs in that stub while completing it: it constructed `new ChartClass(this)` (passing the `Graph3D` instance itself as the chart's `scene` argument, instead of a real `THREE.Scene`) — now `new ChartClass(this.#activeScene.three)`, requiring an active scene first (throws `"no active scene"`, mirroring `g.postfx`'s identical existing requirement/message pattern) since there's nothing to attach chart objects to otherwise. Unknown type names now get a small Levenshtein/edit-distance "did you mean" suggestion (a plain Wagner–Fischer DP implementation, `MAX_SUGGESTION_DISTANCE = 3`) instead of always dumping the full registered-name list — the list remains the fallback when nothing is close enough to guess confidently. `Graph3D.js` importing every concrete `chart/` class directly is a third instance of the same "composition root" cross-layer exception `GraphScene`/`PostFX` already established (CLAUDE.md §1.4) — verified non-circular (`madge --circular src/` clean), since `chart/` only imports back into two `core/` leaf utility modules (`Graph3DLoop.js`/`GraphDisposal.js`), never `Graph3D.js` itself. Existing `tests/core/Graph3D.test.js` tests asserting `'bar'`/`'scatter'` were unregistered (permanently obsolete once real registration landed) replaced with tests dispatching and rendering all 11 real types against a real `GraphScene`, plus the new active-scene requirement and Levenshtein-suggestion behavior. Verified end-to-end in a real browser (a throwaway smoke page, not a numbered example) that all 11 types construct, bind to the active scene, and render without error via `g.chart(typeName)`. `docs/concepts/chart.md` updated
- [x] Per-datum styling accessors — `.opacity(fn)`/`.visible(fn)`/`.size(fn)` (Prompt 141): `.color(fn)` (Prompt 127) already worked across every per-datum chart; this fills in the other three the same "thin sugar over `chart.selection().attr(...)`" way via three shared modules — the pre-existing `chart/opacityField.js`, and two new ones, `chart/visibleField.js` (a direct passthrough to `Selection.attr('visible', ...)`, Prompt 75's already-supported-but-never-chart-exposed vocabulary entry — `GraphChart.visible(fn)` is a new setter mirroring `.opacity()` exactly) and `chart/sizeField.js` (`.size(fn)` as a per-datum *multiplier*, not a replacement: reads each member's current scale off `chart.selection().backend` — the same escape hatch `ScatterChart.pick()` established — and multiplies it by the accessor, writing back through `.attr('scale.x'|'y'|'z', ...)`; safe against compounding across repeated `update()` calls because every consumer here fully recomputes its base scale from scratch every call). Wired into all seven chart types with a real per-datum backend: `BarChart`/`HeatmapChart` restrict `.size()` to the footprint axes only (`x`/`z` normally, `y`/`z` when `BarChart.horizontal()` is active — the value-encoding axis is never touched), `NetworkChart`/`TreeChart`/`PackChart`/`PieChart` multiply all three axes uniformly (spheres/wedges scaled around their own center). `ScatterChart.size()` was deliberately left on its own, more-efficient compute-time mechanism rather than migrated to the shared multiplier (CLAUDE.md §3 Surgical Changes — already correct, already shipped, behaviorally identical either way since its base scale is always `1`). `LineChart`/`AreaChart`/`SurfaceChart`/`VolumeChart` stay exempt, as already documented (no per-datum concept for a continuous path/mesh/field). Two `skipping_list.md` entries from Prompts 137/138/139 (`NetworkChart`/`TreeChart`/`PackChart`'s and `PieChart`'s inert `.size()`/`.opacity()`) resolved (partially — `.shape()`/`.filter()`/`.sort()` remain inert on those five; `VolumeChart`'s inertness is unrelated and unresolved, a genuine architectural consequence of rendering one continuous volume, not N datums). `PackChart.size()` can visually overlap siblings (`layout.pack()` only guarantees non-overlap for un-multiplied radii) — a documented tradeoff of one shared mechanism everywhere, not a bug. Unit tests (new `tests/chart/sizeField.test.js`/`visibleField.test.js`, additions to every affected chart's own test file), verified end-to-end in a real browser (a throwaway smoke page, not a numbered example) that opacity/visibility/size all render correctly for `BarChart` and `NetworkChart`. `docs/concepts/chart.md` updated
- [x] `chart.use(middleware)` data transforms (Prompt 142): a new `compose/transform/` module (`smooth`, `decimate`, `aggregate`, `normalize`, `sort`) — pure `(data) => data` middleware factories, re-exported as the `transform` namespace (`compose/index.js`, `src/index.js`), the same "factory namespace" convention as `generator`/`layout`, but non-chainable (each takes its full config up front — no current prompt needs more, CLAUDE.md §1.3 YAGNI). `GraphChart.use(fn)` (mirrors `.on()` — always registers and returns `this`, no getter form) pushes onto a private `#middlewares` array, run in registration order inside the pre-existing `#prepareData()` (shared by `render()`/`update()`) between `.filter()` and `.sort()`: filter narrows the set first, middleware reshapes it (`decimate`/`aggregate` can change its length, `smooth`/`normalize` its values), then `.sort()` orders the final result — composable, since `.use()` can be called multiple times and `transform.sort` is also available as a middleware for interleaving with the others inside one pipeline. `smooth(window)` deliberately operates on a plain `number[]` with no per-datum field parameter (asked and confirmed by the user rather than guessed) — `GraphChart`'s own default `y` accessor is already the identity function, so bare-number datum arrays are the common case, not a limitation; `decimate(target)` uniform-stride-samples down to `target` (no-op if already smaller); `aggregate(keyFn, reducer)` groups by key (first-occurrence order) and reduces each group to one output datum; `normalize(field)` min-max rescales a named field to `[0,1]`, returning new objects (never mutates input, per CLAUDE.md immutability) and mapping an all-equal field to `0` rather than dividing by zero. Unit tests for all five (`tests/compose/transform/*.test.js`) plus the namespace export, and `GraphChart.use()` tests (registration/chaining/validation, a full filter→use→use→sort pipeline integration test, and the disposed-chart-throws list). `docs/concepts/chart.md` updated
- [x] `chart.legend(options)` + `chart.tooltip(handlerFn)` (Prompt 143): two design forks were asked-and-confirmed rather than guessed. `legend()` mounts only into a caller-supplied `options.container` (duck-typed to `.appendChild`) — the chart never creates or positions DOM elements of its own. `tooltip()` is config-only on every chart type for now: it stores a handler (or `null`), with `chart/tooltipField.js`'s `resolveTooltipContent` defining the "sensible default on hover when no handler is set" content (a `"key: value"` listing for object datums, `String(datum)` otherwise) — no hover-*triggering* mechanism exists yet, since real hover-detection is Phase 9's job (`interact/Tooltip.js`, Prompt 151) and only 2 of 11 chart types (`ScatterChart`/`PieChart`) have any picking mechanism at all today; building ad hoc wiring for the other 9 now would duplicate Prompt 151's real work. `chart/legendField.js`'s `applyLegend(chart)` renders a gradient bar + min/max labels (continuous palette) or a swatch list (categorical, `.categorical === true`) for `.color()`, and three sample dots at the data's min/mid/max `.size()` multiplier — reusing `chart.data()`/`chart.color()`/`chart.size()`'s existing getters rather than threading new state through, clearing and rebuilding the container's content on every call (safe to call unconditionally). Wired into the same five chart-type "sync" spots `.opacity()`/`.visible()`/`.size()` (Prompt 141) already touch — `BarChart`/`ScatterChart`/`HeatmapChart`/`NetworkChart`/`PieChart` — since `GraphChart`'s own `render()`/`update()` are only used directly by the first three; `TreeChart`/`PackChart` stay exempt (documented) because their `.data()` binds a single root object, not an array, so there's no per-datum domain to fit. `GraphChart.destroy()` additionally clears a configured legend's container content (the one DOM resource this feature creates — CLAUDE.md's Disposal Contract), leaving the container element itself alone since the caller owns it. Unit tests (`tests/chart/legendField.test.js`, `tests/chart/tooltipField.test.js`, `GraphChart.legend()`/`.tooltip()` additions including the disposed-chart-throws list and a destroy()-clears-legend test), wiring tests added to all five chart types' own test files. Verified end-to-end in a real browser (a throwaway smoke page, not a numbered example): a `BarChart` legend rendered a correct viridis gradient bar with accurate min/max labels and three correctly-sized/labeled sample dots, re-synced correctly after `update()` with new data, and `resolveTooltipContent` produced the expected default and custom-handler output. `docs/concepts/chart.md` updated
- [x] `examples/19-gallery/` — the homepage hero (Prompt 144): one instance of every Phase 8 chart type (`Bar`/`Line`/`Scatter`/`Area`/`Surface`/`Heatmap`/`Network`/`Tree`/`Pack`/`Pie`/`Volume`) in a single shared `THREE.Scene`/camera on a 4×3 floor grid. (The prompt's literal path, `examples/08-gallery`, was already `08-bar-chart` — numbered `19` instead, continuing the sequence after `18-volume-chart`.) Every chart's rendered objects must attach to a real `THREE.Scene` (`GraphObject.js` validates `instanceof THREE.Scene`, discovered by the first real-browser run throwing immediately) — a `THREE.Group` per cell doesn't satisfy that, so each chart is instead constructed directly against `scene.three`, and a `placeCell(buildFn, settleFn?)` helper reparents whatever objects `buildFn` just added into a `content` group afterward (stock `Object3D.add()` reparenting, no library change). Natural per-chart-type footprints vary by more than an order of magnitude — hand-tuned domains alone left `PackChart`'s recursively-relaxed sphere-packing radius and `NetworkChart`'s charge-repulsion spread many times larger than a unit-cube `VolumeChart` (measured via `THREE.Box3`: ~39 and ~21 world units respectively against every other chart's ~4-5) — so `placeCell` instead measures each cell's actual bounding box after building and rescales+recenters it to one common `CELL_TARGET_SIZE`, uniformly, rather than fighting each layout's own scale. `NetworkChart` additionally takes an optional `settleFn`, pre-ticking its force simulation ~300 times synchronously before that measurement so the captured box reflects the settled layout, not its freshly-seeded starting spread (ongoing `loop.add(() => networkChart.tick())` afterward is then near-motionless, matching every other chart's fully-static cells). Verified end-to-end in a real browser: no console errors, a `THREE.Box3`-based structural check confirmed each of the 11 cells contains exactly the expected child count/type signature for its chart type (e.g. Network = 14 node meshes + 13 `Line2` edges = 27, matching the constructed data), and repeated fresh-navigation checks confirmed `OrbitControls` (enabled, matching every other example) held a stable camera position with zero drift — two apparent bugs chased during this prompt (a "black canvas" and "0 render calls" reading, and an earlier "camera drift") both turned out to be either a normal first-frame-paint delay or a stale browser tab that a same-URL `navigate()` call hadn't actually reloaded, not real defects.
- [x] Phase 8 capstone tests (Prompt 145, `tests/integration/phase8.test.js`): join-hook enter/update/exit dispatch and `.use()` middleware order-stability were already thoroughly covered on the abstract base class (`tests/chart/GraphChart.test.js`, Prompts 128/130/142) and every concrete type already has its own render→update→destroy lifecycle test in this directory — none of that is re-tested here (documented in the file's header, mirroring `phase7.test.js`'s precedent for what a capstone file adds vs. reuses). What's new: (a) a single table-driven smoke test proving all 11 concrete types still render+destroy together (catches a type accidentally broken by an unrelated change in one place rather than 11); (b) 1000× `update()` with fresh data each pass on the instanced backend — a genuine gap, since the existing disposal tests only exercised repeated create/destroy, never repeated *update* on one live instance; (c) 1000× create/render/destroy on a concrete chart type (`BarChart`, not just the abstract `GraphChart` + `generator.bar()` combination `GraphChart-disposal.test.js` already covers), confirming a real subclass's color/style-field wiring doesn't leak either; (d) `.color(accessor, palette)` verified landing in the real `InstancedMesh.instanceColor` `InstancedBufferAttribute` (order-agnostic — counts each expected palette color's occurrences rather than assuming instance index order matches input row order), not just requested through the `Selection` API; (e) a hand-written `chart.selection().filter(...).attr('color', ...)` micro-edit confirmed surviving a subsequent `update()` call for a datum whose bound data didn't change, since `update()`'s default (handler-less) path only ever rewrites `position`/`scale` (`GraphChart.js#writeComputedTransform`), never `color`. Writing (a) surfaced a real, previously-unexercised API nuance (not a bug): `BarChart`/`ScatterChart`/`HeatmapChart` inherit `GraphChart.data()` verbatim, which returns a `JoinResult` (Prompt 128's documented two-in-one join API), not `this` — so `.data(...)` can't be the last call in a chained builder for those three, unlike `LineChart`/`AreaChart`/`TreeChart`/`PackChart`, which override `.data()` as a plain getter/setter returning `this`. Writing (d) surfaced a real test-authoring pitfall: comparing `THREE.Color` instances read back from a `Float32Array`-backed `InstancedBufferAttribute` via `.equals()` (exact `===`) is precision-fragile — fixed with a small per-component tolerance instead. 167 files / 2588 tests passing; lint clean.
- [x] All chart types default to instanced rendering via `GraphInstancedObject`; fall back to mesh for <50 datums — `INSTANCING_THRESHOLD` (`GraphObjectFactory.js`), applied automatically wherever a flat per-datum backend exists (Prompts 129/132/134/136/137/138); the remaining types render one continuous mesh/field or one mesh per slice, for which no instancing choice applies (documented in "The chart contract" above, Prompt 146)
- [x] Fluent API: `.data(arr, keyFn)`, `.x(fn, scale)`, `.y(fn, scale)`, `.z(fn, scale)`, `.color(fn, palette)`, `.material(preset)`, `.transition()`, `.on(event, handler)`, `.render()` — `GraphChart` (Prompt 127), consumed unchanged or overridden-with-documented-exceptions by every concrete type (Prompts 132–139)
- [x] Diff-based updates — only changed instances are written: the originally-planned standalone `GraphChartDataBinding` class was absorbed into `compose/selection/diff.js`'s `diffData` (its own header already anticipated this exact consumer, per CLAUDE.md §1.1 DRY — no separate class was ever built, by design, not an omission), driving `GraphChart.update()` (Prompt 130) and every concrete override's equivalent
- [x] Every chart type has a unit test, integration test, and example page — Prompts 132–139 per-type, plus the Phase 8 capstone (`tests/integration/phase8.test.js`, Prompt 145) and the `examples/19-gallery/` hero showing all 11 together (Prompt 144)
- [x] `graph3d.chart('bar')` dispatch wired in `Graph3D` (Prompt 140) — all 11 type names registered, with active-scene requirement and Levenshtein "did you mean" suggestions
- [x] `docs/concepts/chart.md` documents the full chart contract and the enter/update/exit join lifecycle (Prompt 146, "The chart contract" and "Join lifecycle" sections) — Phase 8 marked complete

---

### Phase 9 — Interaction & State — **DONE**
**Scope:** `src/interact/`  
**Prompts:** 141–165  
**Exit criteria:**
- [x] `Picker` centralized hit-testing (Prompt 147, `src/interact/Picker.js` — the first file in a new `interact/` layer, `src/interact/index.js` barrel added, re-exported from `src/index.js`): `register(chart)`/`unregister(chart)` (duck-typed to `chart.selection()` — works for any concrete chart type, not just the two, `ScatterChart`/`PieChart`, with their own one-off `.pick(raycaster)`) and `pickAt(x, y)` — canvas-local pixel coordinates converted to NDC and cast through the constructor's `camera`, returning the closest hit (`{chart, mesh, instanceIndex, datum, worldPoint}`, `instanceIndex` `null` for a meshes-backend hit) across every registered chart, or `null`. Dispatches per chart on `chart.selection().backend`'s own shape (`Selection.backend`, Prompt 134's escape hatch) rather than building a second spatial index: the octree path for an instanced backend is a new `GraphInstancedObject.pickDetailed(raycaster)` (`src/object/GraphInstancedObject.js`) — refactored to share its private octree-accelerated traversal with the pre-existing `pick()` (CLAUDE.md §1.1 DRY) rather than duplicate it, since `pick()` alone only ever returned the instance index, and `Picker` additionally needs the world-space hit point; the meshes-backend fallback is a plain `THREE.Raycaster.intersectObjects` (mirrors `ScatterChart.pick()`'s own meshes-backend branch). "One pick per frame max" (the prompt's own wording) is a same-`(x, y)`-within-one-frame cache, invalidated by a one-shot callback on the shared `loop` (Prompt 20) rather than a second `requestAnimationFrame` (CLAUDE.md §2 anti-patterns table) — a call at a *different* `(x, y)` within the same frame is never held back by it. Writing this surfaced a real, previously-latent bug (not present in `ScatterChart.pick()`/`PieChart.pick()`'s own existing tests only by coincidence — every one of their hit-datum fixtures happens to sit at the world origin): `THREE.Object3D.matrixWorld` is only recomputed by a real `WebGLRenderer.render()` call, so raycasting a `GraphMesh` positioned away from the origin, before any frame has actually rendered (e.g. a pick requested from a `pointermove` handler that fires before the first paint), silently hit-tests against a stale identity transform instead of the mesh's real position. Fixed inside `Picker` itself (`chart.scene.updateMatrixWorld(true)`, deduped per unique scene across all registered charts, once per `pickAt()` call) rather than in `GraphMesh`/`ScatterChart`/`PieChart` — out of this prompt's surgical scope, not revisited here. This project's older (`v2`-era) exit-criteria wording above ("GPU picking via render-to-texture") doesn't match `prompts.md`'s actual Prompt 147 text, which specifies "Octree path for instanced, raycaster fallback" — the CPU-side approach already established by `ScatterChart.pick()`/`GraphInstancedObject.pick()` in Phase 8, not a GPU color-ID render pass; `prompts.md` is the current source of truth (see its own "v2→v3 prompt renumbering" note), so this criterion is satisfied by the CPU-side design, not superseded by it. Unit tests (`tests/object/GraphInstancedObject.test.js` additions for `pickDetailed`, new `tests/interact/Picker.test.js` covering construction validation, register/unregister, both backend paths, closest-hit-across-multiple-charts, the same-frame cache and its per-`(x,y)` scoping, and the disposed-throws list), new `docs/concepts/interact.md` (Phase 9's first docs page). 168 files / 2605 tests passing; lint clean.
- [x] `StateMachine` per-chart datum interaction state (Prompt 148, `src/interact/StateMachine.js`): fixed state vocabulary `'default'`/`'hovered'`/`'focused'`/`'selected'`/`'dragging'` (the prompt's own wording), tracked per datum via `Map<datum, state>` (only non-`'default'` entries stored — an untouched chart carries zero bookkeeping). Wraps one chart via constructor (`new StateMachine(chart)`, duck-typed to `chart.selection()`, mirroring `Picker`'s own chart-duck-typing) rather than being added onto `GraphChart` itself — despite the prompt's literal `chart.stateOf(datum)` wording, `chart/` sits below `interact/` in CLAUDE.md §1.4's layering table, so `GraphChart` importing `StateMachine` would close a real dependency cycle; a caller wanting that access constructs a `StateMachine` alongside the chart instead (the prompt's phrasing read as "the chart's state machine's `stateOf`", not literally a `GraphChart.prototype.stateOf`, the same resolution CLAUDE.md §10 calls for when a prompt's literal wording would violate the architecture). `style(state, responseFn)` is the "configurable visual responses" the prompt asks for — a two-in-one getter/setter (mirrors `chart.tooltip(handlerFn)`'s convention) storing one response function per state, called with `(selection, datum)` (`selection` pre-filtered to just that datum's node via `chart.selection().filter((d) => d === datum)`) whenever `setState(datum, state)` transitions a datum into that state. No separate "leave" hook: leaving a state is just entering another one (typically `'default'`), and that state's own configured response (if any) is responsible for whatever appearance it wants — declarative, same as `chart.color(fn)`/`chart.opacity(fn)`'s absolute writes elsewhere in `chart/`. `setState` is a full no-op (bookkeeping *and* visuals) if the datum is already in the target state, and a visual-only no-op (bookkeeping still updates) if the datum isn't currently bound/rendered by the chart or no response is configured for that state — neither silently swallows an error, both are legitimate, expected conditions (Fail Fast is reserved for actually-invalid input: an unrecognized state name throws `TypeError` from both `setState`/`style`). Deliberately does not implement pointer/event *detection* (hover-enter/leave comparison, click-to-select, drag-start/end) — that's Prompt 149's job (wiring `Picker` → `StateMachine`) and Prompt 150's default hover/select visuals; `StateMachine` here only stores state and applies its configured response when told to. Unit tests (`tests/interact/StateMachine.test.js` — construction validation, `stateOf` defaulting, `setState`/`style` two-in-one behavior, response-scoped-to-one-datum verified against real mesh material colors, same-state/no-configured-response/datum-not-bound no-op cases; no disposal test — `StateMachine` holds no GPU/DOM/RAF resources of its own, so CLAUDE.md §3's Disposal Contract doesn't apply and it has no `dispose()`), `docs/concepts/interact.md` updated. 169 files / 2619 tests passing; lint clean.
- [x] `Selection.on(event, handler)` + `PointerRouter` wiring (Prompt 149, `src/compose/selection/Selection.js`, new `src/interact/PointerRouter.js`): replaces the Prompt 80 throw-stub with a real, open-vocabulary (any non-empty string, unlike `GraphChart.on()`'s fixed `'enter'/'update'/'exit'`) per-`Selection` event registry — `on(event, handler)` pushes onto `#eventHandlers.get(event)` (accumulates, registration order, mirrors `GraphChart.on()`) and adds `this` to a module-level `eventedSelections` `Set` (a plain `Set`, not `WeakSet`, since dispatch needs to iterate it — see the leak caveat below). The new static `Selection.dispatch(eventName, {mesh, instanceIndex, datum, worldPoint, domEvent})` (`mesh`/`instanceIndex` are `Picker`'s own hit vocabulary, Prompt 147) walks every evented selection, and for each one with a handler for `eventName`, checks membership via a new private `#localIndexOf(mesh, instanceIndex)` (meshes backend: `meshes.findIndex(m => m.three === mesh)`; instanced backend: confirms `object.three === mesh` then `indices.indexOf(instanceIndex)`) — this is exactly what makes "filtering a selection scopes its handlers" (the prompt's own example) work: `.filter()` (Prompt 76) returns a new `Selection` sharing the same underlying `GraphMesh` references / narrowed `indices`, so `#localIndexOf` naturally only matches the filtered subset. `Selection.dispose()` now also removes `this` from the registry (chart-owned root selections, disposed by `GraphChart.destroy()`, correctly stop firing after teardown); an ad hoc `chart.selection().filter(...).on(...)` result that's never stored/disposed keeps its handler(s) — and itself — alive for the app's lifetime, a documented, accepted tradeoff of the prompt's own "handlers register per-Selection-scope" requirement (a scoped selection has to persist *somewhere* for its handler to ever fire later), not a bug this file can silently avoid (`skipping_list.md`). New `src/interact/PointerRouter.js` is the "wire pointer events → picker → state machine" half: `new PointerRouter({picker, domElement})` attaches real `pointermove`/`click` listeners and, per event, calls `picker.pickAt()` then (a) hover-enter/leave — transitions the hit/previous-hit datum via `stateMachineFor(chart).setState(datum, 'hovered'|'default')`, but only when currently exactly `'default'`/`'hovered'` (a `'selected'`/`'dragging'` datum isn't clobbered by a mere hover-past — a state should reflect the strongest current interaction, not the most recent pointer position; `Selection.dispatch('hover-enter'|'hover-leave', ...)` still always fires regardless, e.g. for a future tooltip), and (b) select/shift-multi-select on `click` — no Shift clears every previously-selected datum (across every registered chart) then selects the hit; Shift skips the clear and *toggles* the hit's selection instead, accumulating a cross-chart multi-selection (`Selection.dispatch('click', ...)` always fires for a hit). `stateMachineFor(chart)` lazily creates/caches one `StateMachine` (Prompt 148) per chart — no separate chart-registration step needed on the router itself, since a chart only reaches here by already being `picker.register()`ed. Deliberately doesn't handle drag (`'dragging'` exists on `StateMachine` but nothing here drives it — not asked for by this prompt) or default hover/select visuals (`chart.hoverStyle`/`selectStyle`, explicitly Prompt 150's job) — `PointerRouter` only detects transitions and tells `StateMachine` to make them. `dispose()` removes both listeners (CLAUDE.md's Disposal Contract — the one resource this class creates) and clears its `StateMachine`/selection bookkeeping, without resetting any datum's current visual state. Verified `madge --circular src/` clean (no cycle from `interact/` → `compose/`, allowed per CLAUDE.md §1.4's layering table; `interact/`'s only reference to `chart/GraphChart` is a JSDoc `@type` import, never a runtime one). Unit tests (`tests/compose/selection/Selection.test.js` — replaced the old stub test with `on()`/`dispatch()` coverage across both backends, filter-scoping, multi-handler accumulation, and dispose-deregisters; new `tests/interact/PointerRouter.test.js` — construction validation, `stateMachineFor` caching, hover-enter/leave including the selected-state-not-clobbered case, click/replace/shift-toggle-multi-select across multiple charts (using `Picker`'s own register/unregister to swap which chart is hit, and manually advancing `Picker`'s "one pick per frame" cache via the captured `loop.add` callback — the same technique `Picker.test.js` established — rather than solving exact off-center perspective-camera NDC math), and the disposed-throws/idempotent-dispose cases), `docs/concepts/interact.md` updated. 170 files / 2643 tests passing; lint clean.
- [x] State visuals + hover shader effects (Prompt 150, new `src/material/effects/` — `harness.js`, `registry.js`, `EffectInjector.js`, `PhaseAnimator.js`, `EffectController.js`, `presets/{glow,fire,crackers,lightenup,pulse,ripple,neonEdge}.js`; `src/interact/StateMachine.js`; `src/chart/GraphChart.js`): prompts.md lists Prompt 150 twice back-to-back (a short "defaults only" entry immediately followed by a longer one expanding it into (a) defaults + (b) a full shader-effects registry, with no separate Prompt 151 slot in the file) — user explicitly chose the full (a)+(b) scope via `AskUserQuestion` rather than have this session guess. **(a) Defaults**: every `StateMachine`, even freshly constructed with zero `.style()` calls, now shows a default hover (outline effect + 5% uniform scale) and select (outline variant, no scale) appearance — implemented as logic *inside* `setState()` itself, deliberately **not** stored in the `#styles` map `style()` reads/writes, so `stateMachine.style('hovered')` still reads back `null` on a fresh instance (the pre-existing Prompt 148 test asserting exactly that keeps passing untouched — CLAUDE.md's "never rewrite existing tests unless the test itself was wrong" the deciding factor in this design choice, not an afterthought). The hover scale bump captures each datum's pre-hover scale in a `WeakMap` and restores that exact value on leave (rather than dividing by the bump factor), which is what makes it correct regardless of transition history (e.g. `selected → default` without ever having been `hovered` correctly no-ops, since no base scale was ever captured for that transition pair) — reads/writes go through the exact same public `GraphMesh.getScale/setScale`/`GraphInstancedObject.getInstanceScale/setInstanceScale` methods `compose/selection/attr.js`'s `TRANSFORM_ACCESSORS` table already uses, reached via `Selection.backend` (public getter), so `StateMachine` never touches anything private. `StateMachine.hoverStyle(options)`/`selectStyle(options)` (two-in-one getter/setter, mirrors `style()`) resolve the prompt's literal `chart.hoverStyle/selectStyle` wording onto the `interact/` wrapper — the same resolution pattern Prompt 148 (`chart.stateOf` → `StateMachine.stateOf`) and Prompt 149 (`PointerRouter` as a sibling wrapper) already established for chart-scoped wording that would otherwise violate `chart/` sitting below `interact/` in CLAUDE.md §1.4's layering table. **(b) `material/effects/`**: a registry of 7 named GLSL presets (`glow` emissive halo pulse, `fire` noise-driven flame ramp along local Y, `crackers` flickering surface sparks — documented as a surface-space approximation, not literal ejected particles, consistent with the prompt's own "no particle system dependency" constraint, `lightenup` brightness lift + fresnel rim, `pulse` rhythmic emissive beat, `ripple` radial highlight wave, `neonEdge` glowing silhouette edge — `StateMachine`'s own default), each a self-contained vertex/fragment GLSL chunk pair (sharing one hash/noise helper and one harness scaffold, `harness.js`, extracted once the second/third preset needed it — DRY two-strike rule) applied via `onBeforeCompile` (`EffectInjector.js`) so a material's byte-identical original behavior returns once every effect slot is removed. `effects.list()`/`effects.get(name)` (Levenshtein "did you mean" suggestion on an unregistered name — Fail Fast, no user-authored GLSL accepted in this prompt) live in `registry.js`. Per-datum targeting differs by backend (`EffectController.js`, `applyEffect`/`removeEffect`, taking a `Selection.backend` + local index — the same vocabulary `StateMachine`'s single-datum-filtered selection already has, needing no separate "hit" shape): **instanced** bakes the preset into the chart's one shared material exactly once (idempotent — verified by a `material.version`-bump assertion in `EffectInjector.test.js`, since `THREE.Material.needsUpdate` turned out to be write-only with no getter, a real gotcha caught mid-implementation), driving a per-instance `effectPhase_<slot>` attribute (`GraphInstancedObject.defineAttribute`, Prompt 38, reused rather than hand-rolling a second raw `InstancedBufferAttribute` write path — CLAUDE.md §1.1 DRY) that gates the effect per-instance in one draw call; **meshes** clones the hit mesh's material on first active slot, swaps it in, and restores the exact original reference (disposing only the clone's own compiled program via plain `THREE.Material.dispose()` — never `core/GraphDisposal.js`'s ref-counted `disposeMaterial`, which would wrongly decrement texture ref-counts never incremented for this clone) once every slot on that mesh has faded back out. Both paths animate `0..1` phase over ~150ms via a new shared `PhaseAnimator` (`material/effects/PhaseAnimator.js` — one RAF `loop` tick driving any number of concurrent phase animations by key, mirroring `GraphInstancedObject`'s own bulk-transition convention rather than pulling in `anim/Transition`'s heavier target-dot-path/interrupt-registry machinery, which doesn't fit a "write into an instance attribute buffer + commit" per-frame action). A real bug caught and fixed mid-implementation: `EffectInjector`'s `rebuild()` originally replaced its whole uniforms object on every slot add/remove, which silently orphaned any in-flight `PhaseAnimator` closure still mutating the *old* phase-uniform object reference once a second slot was baked onto the same material — fixed by mutating one long-lived uniforms object in place (add/remove individual keys) instead of ever replacing it wholesale. `GraphChart.hoverEffect(presetName, options)`/`selectEffect(...)` (two-in-one getter/setter, same "config-only, doesn't show anything itself" shape as `chart.tooltip()`, since `chart/` sits below `interact/` and cannot itself detect a pointer event) are validated eagerly against the registry at set-time; `StateMachine.setState` resolves them first (when the chart exposes and has actually configured one, duck-type-guarded since `StateMachine`'s `#chart` is only typed to `selection()`) ahead of its own `hoverStyle`/`selectStyle` default. Fixed a second, pre-existing-test-fragility bug this change surfaced (not a new defect, but newly exposed by it): `tests/interact/PointerRouter.test.js`'s `advanceFrame` helper assumed the *last* `loop.add` call was always `Picker`'s one-shot cache-invalidation callback — no longer true once a click's default select-effect application registers its own `PhaseAnimator` tick *after* `Picker.pickAt()` within the same handler; fixed by snapshotting `addSpy.mock.calls.length` before the click whose pick needs invalidating and targeting the first *new* call after that snapshot (still always `Picker`'s, since `pickAt()` always runs first inside `PointerRouter`'s handlers) instead of `.at(-1)`. Verified `madge --circular src/` clean (`interact/` → `material/` is an ordinary downward import via `postfx/`, not a sanctioned exception). New tests: `tests/material/effects/{registry,EffectInjector,PhaseAnimator,EffectController}.test.js`, plus additions to `tests/interact/StateMachine.test.js` and `tests/chart/GraphChart.test.js`. Known, documented limitation: `lightenup`/`neonEdge` reuse THREE's built-in `vNormal`/`vViewPosition` fragment varyings (present in every built-in material this library ships except `material.basic()`, whose shader has neither) for their fresnel term — applying either to a `material.basic()`-backed chart is unhandled, not silently patched around (`skipping_list.md`). Skipped the prompt's third illustrative wiring form, `chart.on('hover', (d, mesh) => mesh.effect(name))` — only `chart.hoverEffect()`/`selectEffect()` and the default `hoverStyle()`/`selectStyle()` have concrete, testable specs in the prompt text; the imperative form has no further spec and would need a `mesh.effect()` method living somewhere no existing layer can host without a new violation (`object/` can't import `material/`) — logged in `skipping_list.md` with a revisit trigger. 174 files / 2691 tests passing; lint clean (0 errors, 2 pre-existing unrelated warnings).
- [x] `Brush`/`Lasso` screen-space region selection (Prompt 152, new `src/interact/{Brush,Lasso,regionSelect,eventEmitter}.js`): `Brush` (draggable AABB) and `Lasso` (free-form polygon, ray-casting point-in-polygon test — same small self-contained-algorithm approach as `registry.js`'s Levenshtein distance, no dependency) share `register(chart)`/`unregister(chart)` (own `Set`, mirrors `Picker`'s exact convention) and a `pointerdown`/`pointermove`/`pointerup` drag lifecycle. Both emit a real `Selection` **once per registered chart with ≥1 matching datum** rather than once per drag — `Selection.merge()` throws across two different charts/`GraphInstancedObject`s (`compose/selection/combinators.js`, "instanced selections must share the same GraphInstancedObject"), so a single `Selection` genuinely cannot span multiple charts; firing per-chart collapses to exactly one call for the common single-chart case the prompt's own "emit `select` with a real `Selection`" wording describes, and generalizes correctly to many. The shared containment query (`regionSelect.js`'s `matchedIndicesForChart`, used by both — CLAUDE.md §1.1 DRY two-strike rule) projects every datum to canvas-pixel screen space and tests each against the region, then builds the matched `Selection` via the *existing* `chart.selection().filter((datum, index) => matchedIndices.has(index))` — no new `Selection`-backend-construction code needed, reusing Prompt 76's `.filter()` machinery outright. A real correctness bug caught and fixed before this ever ran against real data: `GraphMesh.getPosition()`/`GraphInstancedObject.getInstancePosition()` (the accessors `compose/selection/attr.js`'s `TRANSFORM_ACCESSORS` table already uses for `Selection.attr('position.*', ...)`) decompose the *local* transform only — correct for a relative read-modify-write on the same node, but wrong for screen projection, which needs the true world position; `regionSelect.js`'s `worldPositionOf` instead reads straight off the underlying `THREE.Object3D`/`InstancedMesh` (an instanced member's local instance matrix premultiplied by the batch's own `matrixWorld`), and `matchedIndicesForChart` forces `chart.scene.updateMatrixWorld(true)` first — the same staleness gap `Picker.pickAt()` already closes for its own ray (Prompt 147), since a region query on `pointerup` can equally land between frames. `projectToScreen` discards a datum whose projected NDC `z` falls outside `[-1, 1]` (behind the camera or past the far plane) rather than let a not-actually-visible point spuriously match by `x`/`y` alone. `createEventEmitter(validEvents)` (`eventEmitter.js`) is a tiny shared `on(event, handler)`/`emit(event, ...args)` registry — the third copy of this exact "fixed vocabulary, accumulate, call in order" shape in the codebase after `GraphChart.on()` and `Selection.on()`, neither reusable as-is, which is what justifies extracting it (CLAUDE.md §1.1 DRY). Both classes deliberately render nothing of their own (drag rectangle/lasso path, matched-datum highlighting) — same "detect, don't render" split as `Picker` not drawing a cursor and `PointerRouter` not drawing a tooltip — exposing `'brushStart'/'brush'/'brushEnd'`/`'lassoStart'/'lasso'/'lassoEnd'` progress events (naming chosen to directly match Prompt 156's future "full event surface on charts" vocabulary, so that prompt can forward these rather than reinvent them) so a caller can draw its own overlay. Neither requires a minimum drag distance to activate (a lasso path under 3 points — a plain click — simply encloses no area and matches nothing; documented, not special-cased). `dispose()` on both removes the three pointer listeners and clears the registered-chart set; idempotent. Verified `madge --circular src/` clean. New tests: `tests/interact/{regionSelect,Brush,Lasso}.test.js`, built against an orthographic test camera (`OrthographicCamera(-5,5,5,-5,...)` at `(0,0,10)`) for exactly-predictable world→screen pixel math, covering construction validation, register/unregister, event validation, full drag lifecycle (including the pointermove-without-pointerdown no-op case), containment correctness for both meshes and instanced backends (datum positions pinned via `Selection.attr('position.x', ...)` rather than depending on `BarChart`'s own internal scale math), the per-chart `'select'`-firing/zero-match-skip behavior, and disposed-throws/idempotent-dispose. `docs/concepts/interact.md` updated. 177 files / 2727 tests passing; lint clean (0 errors, 2 pre-existing unrelated warnings).
- [x] `link()` cross-filtering (Prompt 153, new `src/interact/CrossFilter.js`): a single exported function, `link(source, target, { transform })`, not a class — there's no per-link state a caller needs to hold onto beyond the closure `link()` sets up internally (the handler it registers on `source`), so a class wrapper around one operation would be pure ceremony (CLAUDE.md §1.2 KISS). Both parameters are duck-typed, matching every other `interact/` class this phase (`Picker`, `StateMachine`): `source` only needs `on(event, handler)` — today that means a `Brush`/`Lasso` registered against the chart being brushed/lassoed, since both already emit `'select'` with `(selection, chart)` (Prompt 152); a plain `GraphChart` doesn't qualify yet (`GraphChart.on()` only accepts `'enter'/'update'/'exit'` and throws `TypeError` on anything else), but the moment Prompt 156 adds a `'select'` event to `GraphChart` under that identical name/signature, `link()` needs no change at all — this is exactly the forward-compatibility the Prompt 152 event-naming decision was made for. `target` only needs `data()`/`render()` — any `GraphChart`. The prompt's literal `link(chartA, chartB, ...)` wording is read the same way CLAUDE.md §10 already resolves this kind of gap elsewhere this phase (`chart.stateOf` → `StateMachine.stateOf`, Prompt 148; `chart.hoverStyle` → `StateMachine.hoverStyle`, Prompt 150): "chartA" names the *role* (whatever emits the selection), not literally an unwrapped `GraphChart` instance. `target`'s full dataset is captured exactly once, at `link()` time (`target.data()`), and every subsequent `'select'` re-filters from that captured snapshot rather than from `target.data()`'s current (possibly already-filtered) value — narrowing then widening a brush re-derives from the true original rows both times instead of compounding filters on top of each other. `transform(selectedData)` — the prompt's own named option — receives `source`'s currently-selected data array (`selection.data()`) and must return a predicate `(datum) => boolean` run against `target`'s captured dataset; default (`selectedData.includes(datum)`) assumes `source`/`target` share row identity (the common case: two different chart types over the same table), with an explicit `transform` available whenever they don't (e.g. deriving a category-membership predicate instead of comparing object references — covered by a dedicated test). One `source` filtering several targets ("B/C" in the prompt's own wording) is just two (three, ...) separate `link()` calls with the same `source` — no dedicated multi-target API, since repeated calls already produce that behavior for free (CLAUDE.md §1.3 YAGNI). Deliberately out of scope, documented in `skipping_list.md`: no `unlink()` (no event-removal capability exists anywhere in this codebase yet — `GraphChart.on()`, `Selection.on()`, and `Brush`/`Lasso`'s `createEventEmitter` — so `link()` doesn't invent one just for this one case), and releasing a brush/lasso over empty space does not reset a previously-applied filter (`Brush`/`Lasso` deliberately never fire `'select'` for a zero-match region, Prompt 152, so `link()`'s handler is simply never called — the caller resets manually via `target.data(fullRows).render()`). New tests: `tests/interact/CrossFilter.test.js` — constructor-style validation (`source`/`target`/`transform` shape checks), default-transform filtering, re-filter-from-snapshot-not-previous-result, custom-transform filtering, one-source-to-two-targets, and one true end-to-end integration test wiring a real `Brush` drag over one chart into a `link()`-filtered second chart. `docs/concepts/interact.md` updated. Verified `madge --circular src/` clean. 178 files / 2735 tests passing; lint clean (0 errors, 2 pre-existing unrelated warnings).
- [x] Drag-and-drop + keyboard navigation (Prompt 154, `src/chart/GraphChart.js` — `draggable()`; `src/interact/PointerRouter.js` extended; new `src/interact/KeyboardNav.js`; `src/interact/Picker.js` — `camera`/`domElement` getters; `src/interact/regionSelect.js` — `localPositionFromWorld`): **Drag-and-drop** lives on `PointerRouter` rather than a new class — it's the same "pointer events → picker/`StateMachine`" concern `PointerRouter` already owns, so extending it is a natural continuation, not scope creep (CLAUDE.md §3 Surgical Changes, read as "touch only what the change genuinely requires," not "never grow an existing file"). `chart.draggable(true)` is config-only (`GraphChart` can't itself detect a pointer, same shape as `tooltip()`/`hoverEffect()`); `PointerRouter` duck-type-checks it before starting a drag. `pointerdown` on a draggable chart's hit datum transitions it to `'dragging'` (remembering whether it was `'selected'`, to restore correctly) and fires `Selection.dispatch('dragStart', ...)`; `pointermove` repositions it by unprojecting the pointer through `picker.camera` at the datum's captured NDC depth — the plane parallel to the screen through the drag-start point, correct for both perspective and orthographic cameras — writing via `Selection.attr('position.*', ...)` like any other micro-control write (no separate drag-ghost rendering: the write *is* the feedback, mirroring `Brush`/`Lasso`'s "detect, don't render more than that"); `pointerup` fires `Selection.dispatch('dragEnd', ...)` and restores the pre-drag state. A drag suppresses this router's own hover tracking for its duration and the `click` a real browser fires right after the terminating `pointerup` (`#suppressNextClick`), so a drag never also re-triggers click-to-select on the same gesture. `localPositionFromWorld` (new export in `regionSelect.js`, symmetric with the existing `worldPositionOf`) converts the unprojected *world* point back to the local frame `Selection.attr('position.*', ...)` expects — the exact inverse of the local-vs-world bug `regionSelect.js` already had to fix for `Brush`/`Lasso` (Prompt 152); `Picker` gained `camera`/`domElement` getters (mirroring its existing internal usage) so `PointerRouter` doesn't need a second copy of either passed to its own constructor (CLAUDE.md §1.1 DRY). **Keyboard navigation** is a new, separate class, `KeyboardNav` — deliberately not folded into `PointerRouter`: a different event source (`keydown`, not pointer events), a different registered-target shape (an ordered list of charts, not a `Picker` to ray-test), and its own DOM resource (the ARIA live region) — the same "small, focused file per concern" split `Brush`/`Lasso`/`regionSelect.js` already established. Tab/Shift+Tab advances a single roving focus cursor across every registered chart's `data()` (recomputed fresh on every keypress, since data can change between them), in registration order, wrapping at both ends, via `chart.draggable`'s sibling reasoning applied to `'focused'`/`'default'` transitions; `preventDefault()` stops the browser's own Tab focus-shifting, and the canvas gets `tabIndex = 0` if it has none (a `<canvas>` isn't keyboard-focusable by default — without this, no `keydown` would ever reach the listener at all). Enter selects (`'selected'`, replacing `KeyboardNav`'s own previous keyboard-driven selection); Escape clears back to `'focused'` if the cleared datum is still the focus cursor's position, else `'default'` (the ARIA APG listbox/grid convention: Escape drops a selection without losing the user's place) — both are no-ops if nothing is focused/selected, not silently-swallowed errors (nothing to do is a legitimate state, same reasoning `StateMachine.setState`'s own no-op cases already use). Every action calls a real, visually-hidden (`position:absolute; width/height:1px; clip:rect(0 0 0 0)` — not `display:none`, which would also hide it from assistive tech) `aria-live="polite"` `<div>` this class creates via `document.createElement`/`document.body.appendChild` and removes in `dispose()` — the first `interact/` class to touch the real DOM tree at all (every other one only ever adds event listeners), inherent to what an ARIA live region requires, not a scope overreach. `describe(datum, chart)` (constructor option, default a `"key: value"` summary per own-enumerable property) formats the announcement text — written as a small local function rather than importing `chart/tooltipField.js`'s existing `resolveTooltipContent` default formatter of the identical shape, because `interact/` may only import `chart/`'s public surface (`chart/index.js`, which doesn't re-export that internal file, CLAUDE.md §1.4) and because the two genuinely serve different purposes (a configured tooltip handler may return markup unsuitable for a screen reader to read aloud, so this deliberately never consults `chart.tooltip()`). `KeyboardNav` exposes a public `stateMachineFor(chart)` (mirrors `PointerRouter`'s own) so a caller can configure `.style('focused', ...)` (e.g. a focus ring) the same way `PointerRouter`'s hover/select styling works. **Scope, documented in `skipping_list.md`, not silently papered over**: `KeyboardNav` keeps its own `StateMachine` cache, independent of any `PointerRouter`'s — if both are used against the same chart, they don't share "what's currently selected" bookkeeping, since `PointerRouter` exposes no public API for its cross-chart selection set that `KeyboardNav` could read/clear instead; unifying the two would require adding that API first, out of scope for this prompt. New/extended tests: `tests/chart/GraphChart.test.js` (`draggable()`), `tests/interact/PointerRouter.test.js` (new `pointerdown`/`pointerup` listener registration, a full `describe('PointerRouter drag-and-drop', ...)` block — non-draggable/empty-space no-ops, dragging-state + `dragStart` dispatch, position-tracking correctness under an orthographic test camera, drag suppressing hover, `dragEnd` dispatch + state restoration including the was-previously-selected case, no-op `pointerup` with no active drag, and click-suppression-then-recovery), new `tests/interact/KeyboardNav.test.js` (construction/`tabIndex`/live-region-creation validation, register/unregister, Tab/Shift+Tab cycling and wrapping across one and multiple charts with real `StateMachine` state assertions via the new public `stateMachineFor`, `preventDefault` call, Enter/Escape including the still-focused-vs-moved-away Escape distinction, custom vs. default `describe`, and disposed-throws/idempotent-dispose including live-region removal). `docs/concepts/interact.md` updated. Verified `madge --circular src/` clean. 179 files / 2771 tests passing; lint clean (0 errors, 2 pre-existing unrelated warnings).
- [x] `FocusFollower` camera orbit + annotation click interactivity + selection round-tripping (Prompt 155, new `src/interact/FocusFollower.js`; `src/interact/PointerRouter.js` extended — `registerLabel`/`unregisterLabel`, `selectedEntries()`; `src/compose/annotation/label.js` extended — `on`/`emit`; `src/chart/GraphChart.js` — `exportSelection`/`importSelection`; `src/interact/regionSelect.js` — `worldPositionOf` exported): **`FocusFollower`** continuously orbits a `THREE.Camera` around whichever datum is "focused," delegating the actual flight path entirely to `anim/CameraTour.orbit()` (CLAUDE.md §1.1 DRY — no second camera-path engine) rather than wiring itself to a specific focus source: `PointerRouter`'s hover/select and `KeyboardNav`'s Tab cursor are both legitimate ones and there's no single canonical "focus" event yet (that's Prompt 156's unification job), so `follow(chart, datum)` takes the target explicitly, resolving it to a world position via `regionSelect.js`'s `worldPositionOf` (now exported, for the identical reason `localPositionFromWorld` was in Prompt 154 — a second consumer outside `regionSelect.js` itself) through the same `chart.selection().filter((d) => d === datum)` narrowing `PointerRouter`'s own drag gesture already uses. Since `CameraTour.orbit()` itself only ever flies one lap and stops (its own `onComplete` fires once, then `pause()`s), `FocusFollower` restarts an identical orbit around the same center every time a lap's `onComplete` fires — `CameraTour.cancel()`'s documented "never fires again once cancelled" contract is what makes this safe: `stop()`/a redirecting `follow()` call always `cancel()`s the in-progress tour first, so a stale lap's `onComplete` can never race a fresh one. **Label clicks**: `annotation.label(...)` objects gained `on(event, handler)`/`emit(event, ...args)` — a tiny event registry with the identical shape `interact/eventEmitter.js` already provides, deliberately written fresh rather than imported, since `compose/` sits *below* `interact/` in CLAUDE.md §1.4's layering table and `compose/annotation` importing from `interact/` has no sanctioned exception (unlike, say, `compose/selection`'s narrow `anim/` carve-out) — the duplication is the architecturally-correct choice, not a DRY violation, the same resolution `chart/tooltipField.js`'s local formatter already established for `KeyboardNav`'s `describe` default (Prompt 154). Only `'click'` is a supported event today (the prompt's own literal wording, `annotation.label(...).on('click')` — growing the vocabulary waits for a real second consumer, YAGNI). `PointerRouter.registerLabel(label)`/`unregisterLabel(label)` (new `#labels` `Set`, mirrors `register(chart)`'s exact shape) opt a label into `click` hit-testing: since a label has no real mesh to raycast against yet (Phase 6's SDF text), `#dispatchLabelClick` instead projects `label.position` to screen space via `regionSelect.js`'s existing `projectToScreen` (CLAUDE.md §1.1 DRY — no second projection helper) and fires `emit('click', {label, domEvent})` on the closest registered label within a new `LABEL_HIT_RADIUS_PX` (20px) constant, independent of and run before the existing chart-datum click/select logic in the same handler (a label click never touches `#selected`/`StateMachine` bookkeeping). **`selectedEntries()`**: `PointerRouter`'s own private `#selected` `Map` is now exposed as `{chart, datum}[]`, in selection order — resolving the exact gap `skipping_list.md` flagged after Prompt 154 ("no public accessor... adding one speculatively would violate YAGNI"), which this prompt's `exportSelection`/`importSelection` requirement now gives a concrete, current consumer. **`chart.exportSelection(selectedData)`/`chart.importSelection(keys)`**: `GraphChart` cannot itself track "currently selected" (that's `interact/`'s `StateMachine`/`PointerRouter`/`KeyboardNav`, and `chart/` sits below `interact/` — importing either would close a real cycle), so rather than invent a second, chart-owned selection concept that would drift out of sync with the real one, these two methods are pure key↔datum conversions: `exportSelection` maps a caller-supplied list of selected datums (e.g. `router.selectedEntries().map((e) => e.datum)`) through the same `keyFn` passed to the chart's last `data(arr, keyFn)` call (falling back to the datum itself if none was given — the same object-identity limitation a positional join already has elsewhere in this codebase) into portable, `JSON.stringify`-able keys; `importSelection` is the exact inverse, resolving previously-exported keys back against the chart's *current* `data()` entries — the concrete reason either exists: an interactive selection is keyed by datum *object identity*, which breaks across a fresh `data(newRows)` call even when the new rows are byte-identical by value (new object instances), so a caller re-applies whatever visual selected state it manages (`stateMachineFor(chart).setState(datum, 'selected')`) against the *resolved*, post-reload datum objects `importSelection` hands back, not the stale pre-reload ones. Both throw a clear `Error` if `data(arr)` was never called (nothing to export/import against) and `TypeError` for a non-array argument — Fail Fast, not a silent empty-array fallback. Verified `madge --circular src/` clean (`compose/annotation` → `interact/` is never crossed; `interact/FocusFollower.js`/`PointerRouter.js` → `anim/`/`compose/` are ordinary downward imports). New/extended tests: `tests/interact/FocusFollower.test.js` (constructor validation, `follow()`'s not-currently-bound/non-chart-shape throws, camera movement on tick via the same mocked-`loop`/captured-tick-callback technique `tests/anim/CameraTour.test.js` established, continuous re-orbit after a full lap verified by a second `loop.add` call rather than a `loop.remove`, redirecting `follow()` cancelling the prior orbit, `stop()`, disposed-throws/idempotent-dispose), `tests/interact/PointerRouter.test.js` (`selectedEntries()`, `registerLabel`/`unregisterLabel` validation, closest-label-wins/outside-radius/unregister-stops-firing hit-testing under the same orthographic test camera convention `Brush`/`Lasso`/drag-and-drop tests already use, label clicks not disturbing chart-datum selection), `tests/compose/annotation/label.test.js` (`on`/`emit` — handler invocation/ordering/chaining/validation), `tests/chart/GraphChart.test.js` (`exportSelection`/`importSelection` — key derivation with and without a `keyFn`, round-tripping through a simulated `data()` reload with fresh object instances, unmatched-key filtering, both throws, and the destroyed-throws list). `docs/concepts/interact.md` updated (new "`FocusFollower`, label clicks, `selectedEntries()`, selection round-tripping" section; the Prompt 154 `KeyboardNav` scope note updated to reflect `selectedEntries()` now existing, though `KeyboardNav` still doesn't consume it automatically). 180 files / 2810 tests passing; lint clean (0 errors, 2 pre-existing unrelated warnings).
- [x] Full chart event surface + `chart.pickingEnabled(false)` (Prompt 156, `src/chart/GraphChart.js` — `INTERACTION_EVENTS`, `on()` extended, new `dispatch()`, `pickingEnabled()`; `src/interact/eventEmitter.js` — new `dispatchToChart`; `src/interact/{PointerRouter,Brush,Lasso,KeyboardNav}.js` extended; `src/interact/Picker.js` — `pickingEnabled` skip): `GraphChart.on(event, handler)` grew a second event vocabulary alongside its original lifecycle trio (`'enter'`/`'update'`/`'exit'`, still only ever fired internally by `update()`'s own data-join) — `'hover'`/`'select'`/`'deselect'`/`'brushStart'`/`'brushEnd'`/`'lassoStart'`/`'lassoEnd'`/`'dragStart'`/`'dragEnd'`/`'focus'` (`INTERACTION_EVENTS`, the prompt's own literal list), stored in a *separate* `#interactionHandlers` `Map` rather than merged into the existing `#handlers` object: `update()`'s enter/update/exit dispatch and this new external one are genuinely different mechanisms (one auto-fires on every data-join, the other only ever fires when an `interact/`-layer class calls the new `dispatch(event, payload)`), and conflating their storage risked `dispatch()` being able to accidentally re-invoke a lifecycle handler outside `update()`, which nothing should ever do. `dispatch()` itself rejects `'enter'`/`'update'`/`'exit'` for exactly that reason — it's the external-only half of the vocabulary. This is deliberately sugar *alongside*, not a replacement for, two pre-existing systems: `Selection.on(event, handler)` (Prompt 149, per-datum/filter-scoped, but re-registered per `Selection` instance — and therefore silently dropped across a `render()`/`update()` re-bind, since a fresh `Selection` replaces `#backendSelection` every time) and `Brush`/`Lasso`'s own `brush.on('select'|'brushStart'|'brush'|'brushEnd', fn)` (not chart-scoped, since one brush can span several registered charts) — `chart.on(event, handler)`'s new handlers persist on the chart itself across re-binds, closing exactly the gap `Selection.on()` has. Wiring, by `interact/` class (each already had the detecting logic from Prompts 149/152/154 — this prompt only adds one more call alongside each existing `Selection.dispatch()`/emitter-`emit()` call, never replacing it): **`PointerRouter`** fires `'hover'` on hover-*enter* only (not leave — `Selection`'s own `'hover-leave'` still covers that, and the prompt's list has no `'unhover'`/`'blur'` counterpart to pair it with); `'select'`/`'deselect'` on click — a plain (non-Shift) click's "clear every previously-selected datum first" step now also fires `'deselect'` on each chart it clears, not just the toggle-off case; `'dragStart'`/`'dragEnd'` alongside the existing `Selection.dispatch()` calls. **`Brush`/`Lasso`** fire `'brushStart'`/`'lassoStart'` on *every* registered chart the instant the drag begins (a heads-up, since which charts will actually match isn't known yet), but `'brushEnd'`/`'lassoEnd'` only on charts that also get a `'select'` (≥1 match) — paired 1:1 with the pre-existing per-chart `'select'` loop rather than invented as a separate always-fires no-op-for-empty-charts case. **`KeyboardNav`** fires `'focus'` on Tab/Shift+Tab, `'select'`/`'deselect'` on Enter/Escape (Enter replacing a *different* chart's previous selection also fires `'deselect'` on that chart) — mirroring `PointerRouter`'s click semantics for the keyboard modality; `domEvent` is now threaded through `#moveFocus`/`#selectFocused`/`#clearSelection` (previously argument-less) so these dispatch payloads carry it like every other interaction event's does. `dispatchToChart(chart, event, payload)` (new, `eventEmitter.js`) is the shared one-line duck-typed guard (`typeof chart.dispatch === 'function'`) all four call sites use — extracted once the third call site needed the identical guard (CLAUDE.md §1.1 DRY two-strike rule); `PointerRouter`'s own prior private copy (from drafting) was deleted in favor of it. `chart.pickingEnabled(false)` (new, same config-only "chart can't detect a pointer itself" shape as `draggable()`/`tooltip()`, default `true`) is duck-type-checked and skipped entirely inside `Picker.#computePick`'s per-chart loop — never raycast, never a closest-hit candidate — for a large static "backdrop" chart nobody interacts with. Verified `madge --circular src/` clean. New/extended tests: `tests/chart/GraphChart.test.js` (table-driven `on()`/`dispatch()` coverage across all 10 interaction events, cross-event isolation, dispatch rejecting lifecycle events and unrecognized ones, `handlers()`'s lifecycle-only shape unaffected, `pickingEnabled()`, both added to the destroyed-throws list), `tests/interact/Picker.test.js` (`pickingEnabled(false)` skip, plus a farther-chart-wins-over-a-disabled-nearer-one case), `tests/interact/PointerRouter.test.js` (`chart.on('hover'|'select'|'deselect'|'dragStart'|'dragEnd', fn)` firing at the right moments, including the plain-click-clears-everyone `'deselect'` case), `tests/interact/{Brush,Lasso}.test.js` (`chart.on('brush*Start'|'brush*End', fn)` — start-fires-on-every-chart vs. end-fires-only-on-matched-charts), `tests/interact/KeyboardNav.test.js` (`chart.on('focus'|'select'|'deselect', fn)` across Tab/Enter/Escape, including cross-chart deselect-on-reselect). `docs/concepts/interact.md` updated (intro sentence + new "Full chart event surface + `pickingEnabled(false)`" section). 180 files / 2844 tests passing; lint clean (0 errors, 2 pre-existing unrelated warnings).
- [x] `examples/09-interaction/main.js` — three linked charts, brush-to-filter, tooltips, click-select, a live "selected data" panel (Prompt 157). The prompt's literal path collides with the pre-existing `examples/09-line-chart` (Phase 8's chart-type examples already occupy `08`–`18`, `19-gallery` is the Phase 8 capstone) — renumbered to `examples/20-interaction/`, continuing the sequence after `19-gallery`, the identical resolution `examples/19-gallery/` itself used for its own literal-path collision (Prompt 144's own BUILD_PLAN entry above). One shared product dataset (25 rows: `id`/`category`/`price`/`rating`, 5 categories × 5 products) drives all three charts in one `THREE.Scene`/camera, spaced via the same "build against the real scene, reparent into a positioned `THREE.Group` after" technique `19-gallery` established (no auto-fit rescaling needed here — three charts, not eleven wildly-different-footprint ones): a `ScatterChart` (price vs. rating, colored by category) is the brush source; a per-product `BarChart` (same row identity/keyFn as the scatter) is `link()`'s target (Prompt 153) — filters via reference-membership, no custom `transform` needed; a category-aggregated `PieChart` is re-aggregated **by hand** off the same `Brush`'s own `'select'` event (Prompt 152) rather than through `link()`, since `link()`'s target must stay a filtered *subset* of its captured rows, never reshaped into a new aggregate — a genuine, documented limit of `link()`'s row-identity model, not a workaround for a bug. One shared `Picker`/`PointerRouter` (Prompt 147/149) registers all three charts for click-select, using Prompt 150's default gold-outline/scale-bump visuals entirely as-is (zero extra `StateMachine` styling configured) — demonstrating the "works with no configuration" bar those defaults were built to. Hover tooltips are wired by hand off `chart.on('hover', ...)` (Prompt 156, the first real consumer of the new chart-level interaction-event surface outside its own test suite) plus each chart's own `chart.tooltip()` config (Prompt 143) — `interact/Tooltip.js` was never built (`prompts.md`'s own Prompt 151 numbering gap, confirmed absent during Prompt 154), so this example is exactly the kind of "wire it yourself for now" case that config-only method's docs already anticipated. Deliberately "sticky until replaced, hidden on canvas `pointerleave`" rather than hidden on leaving a specific datum: `chart.on('hover', ...)` has no paired "un-hover" event (Prompt 156 fires it only on hover-*enter*), and the `Selection`-level `'hover-leave'` alternative would silently stop firing on `barChart`/`pieChart` after their first `link()`/reset-triggered re-render (a fresh `Selection` replaces the old one on every `update()`) — documented as a real, accepted simplification in the example's own code comment, not silently glossed over. The live "selected data" panel reads `PointerRouter.selectedEntries()` (Prompt 155) on every `chart.on('select'|'deselect', ...)` across all three charts. A brush-drag's terminating `click` can also fire ordinary click-to-select at the release point (`Brush` and `PointerRouter` share one canvas with no coordination between them — a real, honest characteristic of combining the two systems today, not fixed here since doing so would mean adding cross-class suppression machinery to library code, out of scope for an example prompt) — noted in the on-page instructions rather than engineered around. **Verification**: `node --check` and `eslint` both clean on the new file; a local Vite dev server (`vite examples/20-interaction --config vite.config.js`) served and transformed the page/module graph with zero errors. **Not verified in a real rendered browser this session** — the Claude-in-Chrome extension wasn't connected, unlike Prompts 124/144's browser-confirmed examples above; a visual smoke test (does the brush rectangle track the drag, do tooltips position correctly, does the selected-data panel update) is still owed before this can be marked to the same bar those two carry. Full existing suite unaffected (180 files / 2844 tests still passing — examples aren't part of it). No `docs/concepts/` update — this is an example, not a new public API.
- [x] `GraphTooltip` positions a DOM element at the hovered point; user-supplied render function — no dedicated class was built: this criterion (and its "Prompt 151" slot, once referenced by `chart.tooltip()`'s own docs) doesn't correspond to any actual entry in the real `prompts.md` sequence, the same v2→v3 renumbering gap Prompt 147's own entry above already documents for its own criterion. What *is* real and current: `chart.tooltip(handlerFn)` (Prompt 143) stores the user-supplied render function; `Picker`/`PointerRouter` (Prompt 147/149) provide real hover detection; `chart.on('hover', ...)` (Prompt 156) is the real event a caller wires DOM positioning off by hand — demonstrated end-to-end in `examples/20-interaction/main.js` (Prompt 157). The gap that's genuinely still open — no dedicated, centralized, auto-positioning tooltip class — is documented in `docs/concepts/interact.md`'s "What's genuinely out of scope for Phase 9" section and `skipping_list.md`, not silently closed.
- [x] Brush and lasso selection return arrays of matching datum keys — superseded by a real `Selection` per matched chart (see the detailed Prompt 152 entry above), a strictly richer artifact than a plain array of keys ("users apply micro-control to the result directly," the prompt's own wording) while still trivially reducible to one (`selection.each((d) => keys.push(d.id))`).
- [x] Cross-filter: selecting items in one chart highlights matching items in linked charts — implemented as data-level filtering (`target.data()` re-joined to the matching subset, then re-rendered), a stricter and more useful form than visual highlighting alone; see the detailed Prompt 153 entry above.
- [x] Phase 9 cross-cutting integration tests (Prompt 158, new `tests/integration/phase9.test.js`; `src/interact/CrossFilter.js` — a real bug fix, not just a test; `tests/interact/CrossFilter.test.js` and `tests/compose/selection/Selection.test.js` extended): every `interact/` class already has thorough unit-level coverage from its own prompt — this file adds only what those don't, mirroring `phase8.test.js`'s (Prompt 145) own "what's new vs. already covered" header convention. **Picking correctness**: closest-hit resolution *across* backend types (existing tests only ever compare two meshes-backed charts, or check one instanced chart in isolation) and octree spatial correctness among several *simultaneously* on-screen instances (existing instanced-pick tests only ever have one on-screen candidate, so a bug returning "the wrong nearby instance" couldn't have failed them). **State transitions**: a real `pointermove`/`click` — not a direct `stateMachine.setState()` call — driving the default hover/select visual all the way to the actual mesh material (`mesh.material !== originalMaterial`, the same clone-and-swap proof `StateMachine.test.js` already established) and scale; `PointerRouter.test.js` only ever asserted the resulting state *string*, `StateMachine.test.js` only ever asserted the visual from a direct `setState()` call — nothing before this connected the two. **`Brush` matches an octree query**: an instanced chart past `INSTANCING_THRESHOLD`, three known-position datums confirmed individually pickable via `Picker.pickAt()` (the octree-backed path) at their own projected screen coordinates, then a `Brush` drag over the same rect confirmed to match exactly that same set — cross-validating `Brush`'s brute-force per-datum screen projection (`regionSelect.js`'s `matchedIndicesForChart`, which does *not* use the octree at all) against the octree path's independent answer, on the same data. **Cross-filter propagation** surfaced a real, previously-undiscovered bug, not just a testing gap: `CrossFilter.js`'s own doc comment (written during Prompt 153) predicted that once Prompt 156 gave `GraphChart` a `'select'` event, "both already use the identical `'select'` event name and `(selection, chart)` handler signature" — false. `Selection`-emitted `'select'` (`Brush`/`Lasso`) calls `link()`'s handler with `(selection, chart)`, `selection` a real `Selection` with `.data()`; `GraphChart.dispatch('select', payload)` (Prompt 156) calls it with a single `payload` object whose relevant field is `payload.datum` — `selection.data()` would have thrown the moment anyone tried `link(someChart, target)`. Fixed by a new `selectedDataFrom(selectionOrHit)` inside `CrossFilter.js` (duck-typed: `.data()` if present, else `[selectionOrHit.datum]`) — a minimal, surgical fix scoped to exactly the shape mismatch, verified via a real `PointerRouter` click on a plain chart source, *and* a genuine A→B→C propagation test (clicking `chartA` filters `chartB` via `link(chartA, chartB)`; clicking `chartB`'s own now-filtered datum fires `chartB`'s own `'select'`, propagating through a second `link(chartB, chartC)` to filter `chartC` too) — added to `tests/interact/CrossFilter.test.js` (its natural home, alongside `link()`'s other tests) rather than duplicated in the capstone file. **Keyboard nav completeness**: `KeyboardNav`'s own docs already claimed entries are "recomputed fresh on every keypress since data can change between them" but no test had ever actually changed a chart's `data()` mid-navigation — confirmed both directions (shrinking past the old focus index re-clamps without throwing; growing reaches the newly-added entries) work correctly as designed, with no code change needed — a genuine robustness verification, not a no-op test. **`Selection.on` scope-filtering fires only for matching datums**: the existing `Selection.test.js` coverage of this (Prompt 149) always called `Selection.dispatch()` directly, on a meshes backend only — added an instanced-backend counterpart at the `Selection` unit level, plus two true end-to-end tests here (meshes and instanced) proving a *real* `PointerRouter` click only fires the `chart.selection().filter(...).on('click', fn)` handler scoped to the actually-clicked datum, never a sibling's. Verified `madge --circular src/` clean. 181 files / 2857 tests passing (up from 180/2844); lint clean (0 errors, 2 pre-existing unrelated warnings). `docs/concepts/interact.md` updated (the `link` section's now-resolved Prompt 156 forward-reference, and the intro paragraph).
- [x] Document Phase 9 in `docs/concepts/interact.md`; mark DONE (Prompt 159 — the prompt's own literal filename, `interaction.md`, doesn't exist; `interact.md` is the real file this whole phase has documented into since Prompt 147, the same resolution CLAUDE.md §10 calls for elsewhere in this phase for a prompt's literal wording vs. the actual architecture). `docs/concepts/interact.md`'s title changed from "(in progress)" to "(complete)" (matching `docs/concepts/chart.md`'s own closing convention from the immediately preceding phase); a new closing "What's genuinely out of scope for Phase 9" section (mirroring `docs/concepts/material.md`'s own Phase 6 precedent) prose-summarizes the highest-signal accumulated gaps from `skipping_list.md` — no `GraphTooltip` class, `KeyboardNav`/`PointerRouter` selection bookkeeping not shared, `Brush`/`Lasso` `domElement`-only listening, `link()` has no `unlink()`/empty-region reset, `FocusFollower` has no automatic focus source, no headless-GL rendering in this test suite — rather than leaving a reader to infer completeness from silence. Also fixed a stale forward-reference discovered while closing out this phase: `docs/concepts/chart.md`'s own Prompt 143 section (`chart.tooltip()`) still promised "Phase 9's `interact/Tooltip.js` (Prompt 151)" as the real, centralized tooltip-display system to come — that class was never built (the "Prompt 151" slot was renumbered out of the actual `prompts.md` sequence entirely, discovered during Prompt 154's own work), so the paragraph now correctly points to `chart.on('hover', ...)` (Prompt 156) plus `examples/20-interaction/main.js` (Prompt 157) as the real, current, manually-wired story — resolving BUILD_PLAN.md's own matching `GraphTooltip` exit criterion above the same way (`[x]`, with an honest note, not silently left unchecked or falsely claimed complete). **Phase 9 — DONE.** 181 files / 2857 tests passing (unchanged — this prompt is documentation-only, no source/test files touched); lint clean; `madge --circular src/` clean.

---

### Phase 10 — Streaming & Scale
**Scope:** `src/stream/`  
**Prompts:** 166–190  
**Exit criteria:**
- [ ] `DataStream` accepts `AsyncIterable<Chunk>` and pipes into live chart updates
- [ ] LOD (level-of-detail) decimation triggered when instance count exceeds configurable threshold
- [ ] GPGPU aggregation computes spatial histograms and bin statistics on the GPU
- [ ] Origin-shifting maintains float32 precision for coordinates > 1 km from origin
- [ ] Worker tasks registered for: sort, decimate, aggregate, spatial-bin

---

### Phase 11 — Integration & Examples
**Scope:** `examples/`, cross-layer wiring  
**Prompts:** 191–220  
**Exit criteria:**
- [ ] Examples for all 8 chart types render without console errors
- [ ] All examples reach 60 fps with 100 000 datums on a mid-range GPU
- [ ] Cross-chart example: scatter + bar + heatmap linked via cross-filter
- [ ] `npm run bench` baseline updated; no regression vs Phase 8 baseline
- [ ] `docs/` covers all public layers

---

### Phase 12 — Publishing
**Scope:** `types/`, `dist/`, CI  
**Prompts:** 221–244  
**Exit criteria:**
- [ ] `npm run build` produces all four bundles (ESM, UMD, minified ESM, minified UMD) with zero warnings
- [ ] `npm run test:coverage` meets ≥ 85% lines, ≥ 80% branches, ≥ 85% functions across all layers
- [ ] `types/index.d.ts` covers every public export; TypeScript `strict` compilation passes
- [ ] Bundle size: ESM minified ≤ 50 KB gzipped (excluding `three`)
- [ ] `npm run lint` exits 0 with zero `eslint-disable` suppressions in `src/`
- [ ] `CHANGELOG.md` documents all public API changes since v0.1.0
- [ ] `npm publish --dry-run` succeeds with correct `exports` map

---

## "Are we done?" Checklist (per phase)

Before marking a phase complete, confirm:

1. All exit-criteria checkboxes above are ticked.
2. `npm test` passes — no regressions in prior phases.
3. The playground (`npm run dev`) boots with zero console errors.
4. No new dependency added without explicit user approval.
5. Definition of Done in `CLAUDE.md §4` is satisfied.
