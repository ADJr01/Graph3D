node_modules/
dist/
.vite/
coverage/
*.log

# VitePress build output/cache (site/ source markdown is committed, this isn't)
site/.vitepress/dist/
site/.vitepress/cache/

# scripts/syncPlaygroundLibrary.js's copy of dist/graph3d.esm.min.js — a
# build artifact regenerated by predocs:dev/predocs:build, not source.
site/public/

# Local dev-only test assets
HDRI/

# Claude/dev-strategy planning files — internal roadmap, not part of the shipped library
.claude/
prompts.md
skipping_list.md
BUILD_PLAN.md